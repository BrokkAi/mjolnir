# Handoff: launch verification campaign (2026-09-23/24)

For the next coordinator (Fable). Read this first, then `.agents/docs/launch-verification-2026-09-23.md` (the runbook: tracks, evidence standard, Run 1/Run 2 records, fix-wave record).

## Standing rules from the user

- **Subagent cap: three Opus subagents at once** (raised from one by the user on 09-24 afternoon: "go ahead and go to 3 opus subagents"). Count resumed agents too.
- Fable designs, triages and reviews; implementation goes to Opus/Sonnet subagents in isolated worktrees; the coordinator cherry-picks onto `master`, runs the affected crates' `cargo test` + `cargo clippy --all-targets -- -D warnings` + `cargo fmt --all -- --check`, then pushes.
- Authorized to file and fix issues as found, commit on `master`, and push (the user asked to keep master current).
- Out of scope: `morannon-podman` (de-risked); macOS (issue #1135, user drives it on a MacBook).
- R5-1 landed (`5cd9c555`): a lab with only `MJ_DATA_DIR` now gets its own SSH socket directory (`$XDG_RUNTIME_DIR/mjolnir/<16-char fingerprint>`); `MJ_INSTANCE` is no longer needed for that.
- Never touch the user's default Mjolnir instance or `target/` for evidence (mbx evicts `target/`). Campaign root: `/home/jonathan/mj-campaign/launch-2026-09-23/` (`bin/` = original campaign build `8b7b1120`; `bin-fixed/` = current fixed build; `evidence/` = all captures and findings).

## State right now (updated 2026-09-24 morning)

- `master` = `origin/master` = `d501dcf9` (merge of origin into the 13 campaign commits; conflicts were in README, acp-agent.md, cli-reference.md, dashboard/actions.rs, tests/acp.rs, resume.rs, plus one semantic conflict in dialogs.rs where origin added `delete_branch_available` to `ForceDestroy`). Validated: default-member `cargo test` (42 binaries, 0 failures), clippy, fmt, web e2e (92 passed, 3 skipped). Pushed.
- Step 2 (macOS CI test) is done: origin's `e8afa5c9` already asserts `invalid peer certificate`; no user "go" was needed.
- `bin-fixed/` rebuilt from `d501dcf9`: `mj` `dc00467d`, `mj-worker` `2463f5c0`, musl worker `328b7498` (`bin-fixed/SHA256SUMS-d501dcf9.txt`).
- #1136 closed by its commit; `agent-in-progress` removed.
- R2 (F/G/H) done: `evidence/luna-manual-seed-3401-3191736/reverify-2/notes.md` (mission text: `evidence/reverify-2-mission.md`). Fix wave R2-A landed in full (R2-6, F-7, R2-5, R2-14, R2-4, R2-3, R2-9, R2-8, R2-10, R2-7; `c69618f4..aa89c000`), validated (controller, cli, core, tui tests; clippy; fmt), pushed; master = origin = `526ab788`. `bin-fixed/` rebuilt from `dd9e55ce` after R2-B (`SHA256SUMS-dd9e55ce.txt`; all three stamped `2.20.0+dd9e55ce…`).
- Fix wave R2-B landed (R2-2 `5f732831`, R2-1 `5dafd135`), validated, pushed; master = origin = `dd9e55ce`. R2-11 cause confirmed, fix deferred (see the runbook's R2 fix waves paragraph and the open list). Its worktree `agent-ab36fc8a205e13311` is still locked by the agent; remove it when unlocked. #1144 filed for the suspend handler's `Controller::load()` and the test that reads the real store.
- Stale agent worktrees and branches from the 09-23 fix waves were pruned on 09-24 at the user's request. The J-25 work in progress from the Track J worktree is saved as `evidence/track-j/j25-codex-quota-error-wip.patch`. Branch `worktree-agent-aa0dbae3ef12d579e` (pre-campaign "WIP: partial ACP 2.0 upgrade") was left for the user.
- R3 done: `evidence/reverify-3/notes.md` (EC2 cleaned up; the ledger directories keep the tool-generated key for a key pair that no longer exists). R4 done: `evidence/reverify-4/notes.md`. Three fix waves are running in parallel in isolated worktrees: R4 (`evidence/fix-wave-r4-mission.md`: R4-2/R4-3 blockers, I2-7, R4-4, R4-1, R4-5, R4-10, R4-7, R4-6, R4-9, R4-11, R4-12, R4-13), R3 (`evidence/fix-wave-r3-mission.md`: R3-3, R3-1, R3-2, R3-8, R3-7, R3-6, R3-5, J-24, J-25, R3-10, R3-9, R3-11), R5 landed (`62a232fe` per-host SHA-keyed worker cache shared with the container path, `cp` per session, EC2 unchanged; `ab7d7f49` index-before-destroy with a 20 s bounded sync wait, a single-session direct write, and a 5 s/1 h deferred retry when the index is busy; controller 1667 tests green; pushed). R3 wave landed (`f15ebc51..eea74e3f`, full suite green, pushed). The host crashed again on 09-24 ~21:00 (reboot); the R4 and R6 agents were resumed from their worktrees: R4 had all 14 commits (`fc72aab4..8143c2b4`) and needed only its final checks and report; R6 had four commits (R4-8, B-11, A-4/E-9, D-14) with I2-10 and I2-1 half-edited. R4 landed (`c0f3babc..4c5f12f6` + `95186525`; one conflict in `mj-worker/src/acp/drive.rs` between R4-7 and J-25 resolved by scoping the usage-limit label to `auth_required` errors), full suite green, pushed. R6 landed (`17a0618f..6168e39b`) with the test reconciliation `afc17876`; master = origin = `afc17876`. Running now: R7 (`evidence/fix-wave-r7-mission.md`: keep Claude Code's `~/.claude/skills/synced` and `.trash` out of Mjolnir's skills sync (user decision 09-24: "let CC do its own sync of that"); rework I2-1 so Codex keeps titling from the user's text (user: losing Codex titles is obnoxious)), and a review-host follow-up (the preparation capture waits for background work and retries on a lifecycle refusal). The review-host follow-up landed (`66888613`). R5 (final fake-lab re-verification, binaries `54925205`) is done: `evidence/reverify-5/notes.md`; R7 (`2d9ed5eb`, `b5ae15db`), the skills follow-ups (`864ae4f8`, `92c34b27`) and R8 (`5cd9c555..25c6af83`) landed. `bin-fixed` rebuilt from `921b52d9` (`SHA256SUMS-921b52d9.txt`). R6 and R7 done (`evidence/reverify-6/notes.md`, `evidence/reverify-7/notes.md`). R9 (`78245c3a..8e3ea78d`) and R10 (`2f8ec62e..6492fad5`, R7-5 resume blocker) landed; full suite, web suite, clippy, fmt green; master = origin = `821beb8c`; `bin-fixed` rebuilt from it (`SHA256SUMS-821beb8c.txt`). R8 done (`evidence/reverify-8/notes.md`): R9 and R10 confirmed; R7-5 resumes now, but the fallback path drops replies (R8-1) and refuses `haiku` (R8-2); fix wave R11 landed (`ccc16ce8..c3d2ae5a`), full suite green, master = origin = `6b87e789`; `bin-fixed` being rebuilt from it; R9 done (`evidence/reverify-9/notes.md`): every R11 fix confirmed on real Claude; fix wave R12 landed (`b1ba33fb`, `b52a038c`). The session-menu request landed (`2a83f0b7`, `41dfe21a`). The Profiles/Targets pane request landed (`ee2d4fcf..e2ec87cd`: the Quota pane is titled Profiles with a blank first column heading; `.` or a click on the Targets/Profiles title opens a Refresh/Settings… menu; Refresh is the shared prefix+shift+r refresh, Targets Settings… opens the Runtimes page; enum names and config keys unchanged; screenshots not regenerated). Full suite and web suite green (one timing test in mj-worker failed under load and passed alone); master = origin = `e2ec87cd`; `bin-fixed` rebuilt from `a45f4a43` (`SHA256SUMS-a45f4a43.txt`). The Targets menu now reads Refresh, Runtimes…, Machines… (`37255577`, user's 09-25 request). Origin's `aa57348b` released 2.21.0; master (2.21.0) = origin = `3eb9ec84`; the campaign binaries in `bin-fixed` are the 2.20.0 build from `a45f4a43`. R4-14 accepted by the user on 09-25 ("fine"). R4-15 answered: the `~/.codex/projects/hel-*` directories are Mjolnir's per-session project-memory replicas (`MEMORY.md` plus notes), placed in the profile home for the memory MCP, 37 of them not cleaned up; user's decision 09-25: every localhost session runs from a staged profile home, including Claude on macOS (the #20553 carve-out was about profile isolation, not staging); fix wave R13 landed (see the runbook): note that at daemon start it removes the `memory/` and `.hel-memory-baseline/` of `projects/hel-*` replicas in configured profile homes whose session is neither in that instance's store nor named by a running process, so the first start of the new build on the default instance will clean the 37 under `~/.codex` and the others listed in the runbook; transcripts stay. R10 done (`evidence/reverify-10/notes.md`): every deferred Codex item confirmed on `codex4`; R14 (`936d6ee9..ebcd71d2`) and R13 (`23af4f3f..1eefcdfc`, every localhost session staged) landed; full suite and web suite green; master = origin = `1f82bd53`. The previous build (`a45f4a43`) is kept at `bin-prev/` for the upgrade-transition check; `bin-fixed` being rebuilt from `1f82bd53`. R11 done (`evidence/reverify-11/notes.md`): R13, R14 and the upgrade transition all confirmed live; R15 landed (`91244ea1..e5b2818d`), full suite and web suite green, master = origin = `da6ab2ee`; `bin-fixed` rebuilt from it (`SHA256SUMS-da6ab2ee.txt`, 2.21.0). R12 done (`evidence/reverify-12/notes.md`): every R15 fix confirmed live. The runbook now ends with a campaign summary and the final open list. R16 landed (`5a09877c..e9fa6be0`), full suite green, pushed; `bin-fixed` rebuilt from the tip. R13 done (`evidence/reverify-13/notes.md`): the public install works; twelve first-run rough/docs findings in fix wave R17 (`evidence/fix-wave-r17-mission.md`, running). The skills-archive gzip change landed (`56e1e39b..89170ef6`) and the archive-restore wizard fix (`9332aae3`); master = origin = `c0731221`. R17 (`caf9d80a..3374ef7d`) and the submodule checkpoint fix (`4d80f0eb`) landed and pushed. The subdirectory-project snapshot fix landed (`c4322938`, archive schema 8; the user's imported session should take a fresh checkpoint on the new build). The live-resume double-press fix landed (`50b85cae`). R14 done (`evidence/reverify-14/notes.md`): R17 confirmed in an agent-free environment; the Codex title check on 1.13.3 stopped at OpenAI's `invalid_refresh_token` on `codex4`, which needs a `codex login` before the last two prompts can run (about 5 minutes; `evidence/reverify-14-mission.md` Part 1). The sub-agent suspend redesign landed (`918770ef..b86d9b71`, migration 52); `bin-fixed` rebuilt from `312cb28c`; R15 (`evidence/reverify-15-mission.md`, real Claude on `launch-r15` port 3787) is checking it live: stop instead of cascade, the warning surfaces, the resume note, the handed-back case, and discard-since-checkpoint. Fix wave R20 landed (`1fb50fe0..0870826d`). R15 done (`evidence/reverify-15/notes.md`): the suspend redesign works live; R22 (`d705260d..1349c768`) and R21 for #1160/#1132 (`c4e2838d..1680ccab`) landed and pushed. The acknowledgement bug was real and is fixed (`f1145012`). No agent running; `bin-fixed` rebuilt from the tip. The campaign is closed unless the user queues more. Open for the user: close #1160 and #1132 (fixed, commented); `codex4` needs a `codex login` to finish the last two Codex checks; the Bifrost question (parked); macOS #1135 (`evidence/fix-wave-r19-mission.md`: stop children without a checkpoint, warn, tell the model on resume). Re-check I2-1 on codex-acp 1.13.3 (origin's `f17b6bcc`) at the next Codex run. The user dropped Bifrost from the list for now (09-25). Bifrost story answered for the user: it is a first-party tool installed only in the container image (`cargo install brokk-bifrost@0.10.7`), found on PATH elsewhere with no installer, doctor check or docs; options offered (doctor check, ship it as a release asset, vendor). The campaign is otherwise closed unless the user queues more; the runbook's "Campaign summary" holds the final open list. Host-environment fixes the user has not yet approved: upgrade `~/.cargo/bin/bifrost` (0.7.5) to 0.10.7 or newer so automatic reviews can analyze; move or delete `~/Projects/aparente-claude-skills/skills/tufte-viz/demos/` (a 2.2 MB file over the 1 MiB skill-file limit); delete the stray `~/.kimi-code/kimi-code.json` left by the Kimi credential bug R13 fixed. Remaining: (I2-1 titles, I2-7, the Codex halves of I1-11/I1-17, I2-9, I2-15, and R11's model-before-mode order on Codex), the untested public install path, and the user's decisions on R4-14 and R4-15. The user's 09-25 wizard request landed (`ccbcf530`: New, Resume and Move skip the target step when exactly one bare target is offered; a lone container or EC2 target keeps its step because that step holds the CPU/memory controls; the step counter drops the hidden step; the web viewer's new-session flow is unchanged because `ViewerTarget` publishes no availability). `tests/e2e/tui_components_actions.py` and `tui_mouse_commands.py` still wait for "1/4 profile"/"2/4 target" and only match on a host with Podman. Then: a real-harness/EC2 spot check of R4-2/R4-3, I2-7, I2-1, R3-7, the SSH worker cache and stale locks once the Codex quota resets on 09-26; then close. Cherry-pick each as it lands (expect small conflicts in `backend.rs`/`resume.rs` between R3 and R5), validate, push, rebuild `bin-fixed`, then one final re-verification of the three waves on fresh labs (F/G/H-style plus one EC2 host plus the real harnesses once the Codex quota resets on 09-26), then close. Mission texts: `evidence/reverify-3-mission.md` and `evidence/reverify-4-mission.md`.

## Next steps, in order

1. ~~Merge origin/master~~ done (`d501dcf9`).
2. ~~macOS CI test fix~~ landed upstream in `e8afa5c9`.
3. ~~Rebuild `bin-fixed/`~~ done.
4. ~~R2 re-verification~~ done; cherry-pick fix waves R2-A and R2-B, validate, push, rebuild `bin-fixed/`. Original: (one subagent): re-run the reproductions of every fixed F, G, H finding against `bin-fixed`. Findings files: `evidence/luna-manual-seed-3206-340978/track-f/findings.md`, `…3207-341010/track-g/findings.md`, `…3208-2579821/track-h/findings.md`. Model the prompt on R1 (results in `evidence/luna-manual-seed-3301-2541214/reverify-1/notes.md`). Note for Track G: the lab needs `phone_tls=True` for a QR URL; Web dialog is `prefix+u`. For H, download old releases into `evidence/releases/`, never `target/`.
5. **R3 re-verification** for J (needs one EC2 host; `tests/e2e/ssh_docker_lab.py`, back up the ledger right after `create`, cleanup by run tag if lost) and the new-in-group-16 behaviors (mandatory `--workspace`, Jev switch off).
6. **Real-harness re-check** (one subagent, isolated instance with `[phone] bind` on its own port, `version = 13`): the items flagged "needs live check": I1-3 `/model claude-opus-5-5`, I1-6 same-profile resume keeps model, I1-11/I1-13 `/clear` incl. after resume, I1-15 review failure reported, I1-17 interrupted marker, I2-7 never-prompted Codex resume, I2-15 Kimi Esc closes permission form, I2-5 command in permission form, #1136 Kimi on a container target, J-19/J-17/J-21 on SSH.
7. **Close the runbook**: fill the Run 2 section's I2 line and the re-verification results, list what stays open, commit, push. Close #1136 if its commit didn't (`Fixes #1136`), remove `agent-in-progress`.

## Still open (not fixed; decide or schedule)

- **Public install path unverified** (R3 runbook error 1): the harness refused `curl … install.sh | bash` and the installer's PATH prompt for the subagent. Either grant that permission for one run or run the README install on a fresh Ubuntu 24.04 host by hand and read the first-run path; the R3 notes say what to look for.
- R4-14: `TYPESAFE_API_KEY` reaches the ACP bridge environment (inside containers too), so the agent's own tools can read Mjolnir's Jev key; the Jev-off switch removes it, but with Jev on it is exposed. Decide whether the worker should keep the key to itself.
- R4-8: `~/.claude/skills/tufte-viz/demos/sunspot-pretty.html` (2.2 MB) exceeds the 1 MiB skills_state limit and every worker logs a failure per minute; a user-environment issue, but the product should log it once, not per minute.
- R4-15: Mjolnir-run Codex sessions keep state under the user's real `~/.codex/projects/`; the Import tab lists a native sub-agent thread. Decide whether that is intended.
- The Codex account (`codex`) is over its limit until 2026-09-26 12:42 and Kimi's weekly quota is spent; the Codex halves of I1-11/I1-17, I2-9 and I2-15 wait on that.
- R3-4 / J-16: every SSH-bare session uploads its own worker copy (139 MB, ~7 min per create under load); needs a shared per-host worker cache keyed by build stamp. Design work, not a wording fix.

- I2-14 Muse question delayed ~3 min (needs logs for the window).
- J-24 container mount source not validated; J-25 Codex quota error shown raw (partial work: `evidence/track-j/j25-codex-quota-error-wip.patch`). J-22 docs done (`5bf82223`).
- R2-11: destroy does not index the session first, so one created and destroyed between sync passes is never findable. Proposed `wiki().sync_now(false)` before the destroy lifecycle; needs a decision on bounding the wait behind a running first index build.
- R2-12 (open 2.19 `mj wait` fails with 503 during an upgrade; accepted trade-off of the H-2 fix), R2-13 (viewer `/api/snapshot` bursts; harm unknown).
- Resume's rollback (`controller/resume.rs`) has the same cleanup-before-save order R2-2 fixed in provisioning; `mj resume` on a failed never-prompted launch is accepted then fails in the background with "session has no checkpoint".
- I2-1, I2-10, A-4/E-9, D-14, B-11: in fix wave R6 (see above).
- Flaky under full parallel load (pass alone): `worker_environment.rs` re-exec test, controller `web_viewer::tests::retry_uses_the_original_port_after_its_owner_releases_it`, `mj-cli/tests/store_divergence.rs`, and (seen 09-24 after merging `69e2f7a6`) controller `move_session::tests::terminal_move_recovery_finishes_interrupted_close_before_phase_retry` and `in_place_move_recovery_after_restart_during_swap_rolls_back_to_stopped`. Also `mj-worker` `verdict_client::tests::continuous_overall_activity_does_not_postpone_or_invalidate_parent_check` (timing). And while several agent worktrees build through the shared mbx cache, the controller's `in_place_move_*` tests can fail with `MJ_WORKER_BINARY is not a file: /mnt/optane/mbx-cache/out-dirs/…/fake-worker.sh` (the build-time fixture's out-dir moved under them); they pass alone. That is the mbx cache, not the code; do not change mbx or `target/` for it.
- Since origin's `51dccacd` (#1138) the controller refuses a worker whose `version+commit` stamp differs from its own. Every `bin-fixed/` rebuild must build `mj`, `mj-worker` and the musl worker from the same commit in one go, and the old-release labs (Track H) are unaffected because they use their own workers.

## Useful mechanics

- Conflict helper for "both sides appended tests": `/tmp/claude-1000/-home-jonathan-Projects-mjolnir/54c16526-8fdf-497a-93bb-dd889fe4a87f/scratchpad/join_conflict.py <file>` (joins HEAD block then incoming block). Use only on test files; resolve code conflicts by hand — it produced a broken `api_client.rs` once.
- Subagent worktrees often start from a stale base (`18eeb2f0`); tell every agent to branch from master's tip.
- The harness often refuses subagents' `findings.md` writes; have them return findings text and write the file yourself.
- Lab cleanup: `mj daemon stop` leaves workers running and they carry no `MJ_DATA_DIR`; kill by exact PID matching the lab runtime path in `/proc/<pid>/cmdline`.
- Memory notes for this project live in `~/.claude3/projects/-home-jonathan-Projects-mjolnir/memory/` (campaign scope, lab mechanics, the subagent cap rule).
