# Launch verification campaign, 2026-09-23

This is an agent-facing runbook for exercising every user-visible surface of `mj` before its public launch, in real terminals, a real browser, and on a real cloud host. It has two purposes at once: **regression** on everything that landed between the close of the last campaign (`.agents/docs/tmux-verification-2026-09-19.md`, 2026-09-19) and the campaign commit — 133 commits, about seventy of them user-visible — and **coverage** of the whole surface as a person who has never used the program would meet it. Unit, render, Playwright and e2e tests cover most of it; this campaign is for what they cannot see.

The campaign is disposable and parallel. Each track is one Opus worker with one isolated lab (or one isolated `--instance`) and, where it needs a terminal, one private tmux server. The coordinator (Fable) prepares the binaries and this document, reads every finding, triages, files issues, dispatches fixes as separate agents after the campaign, and re-verifies. **Workers do not fix anything.** A worker that finds a bug records it and moves on; a finding is only useful when its exact keys, commands, and captures are saved so it can become a regression test.

Two shapes are out of scope by the user's decision: the `morannon-podman` target (SSH + Podman on the user's own host), which the user runs every day and treats as de-risked; and macOS, which cannot run here and is covered by a GitHub issue the user drives on a MacBook (see *Track M*). Also out of scope because nothing here can produce them: a real subscription-quota exhaustion and reset (72dbefee, the 16 h skip in 1d57913e), a Claude safety refusal on demand (30c98758), and the SQLite-busy race (3c668c3d). Record them as not exercised.

## Terms

`mj` is the dashboard binary. The **campaign commit** is `8b7b1120` on `master` in `/home/jonathan/Projects/mjolnir`; the coordinator built three binaries there and copied them to the **campaign root** `/home/jonathan/mj-campaign/launch-2026-09-23/`, outside the repository, because `target/` on this host is a build-cache-managed directory that the cache evicts on its own schedule (it did so during the first run, taking every track's binaries and evidence). Workers use only the copies and never rebuild:

| Binary | Path | SHA-256 (first 8) |
| --- | --- | --- |
| host `mj` | `<root>/bin/debug/mj` | `f78ccc09` |
| host worker | `<root>/bin/debug/mj-worker` | `844e2f63` |
| portable worker | `<root>/bin/x86_64-unknown-linux-musl/debug/mj-worker` | `ec2550bf` |

The layout matters: the daemon finds its worker as a sibling of `mj`, and the portable worker at `../x86_64-unknown-linux-musl/debug/` relative to `mj`'s parent. Evidence lives under `<root>/evidence/`, never under `target/`.

The **prefix** is `ctrl+b`: press it, release, then a second key; `prefix+g` means `ctrl+b` then `g`. A **session** is one agent conversation; a **workspace** is a named group of sessions drawn as a tab across the top of the dashboard. A **target** is where a session's worker and harness run (the built-in `localhost` bare target, a container, or a remote host over SSH). A **profile** is one configured coding agent account (`kind` claude, codex, muse, grok, or kimi). The **fake ACP** is a scripted stand-in for a coding agent that the lab wires in as profile `fake` on target `localhost`; it answers every prompt with `reliability reply: <text>` after a fixed delay, honours cancel, advertises no image support, and **cannot** ask a question, request permission, or fail a turn. The **daemon** is the control plane (`mj daemon-run`, one per instance) that owns the database and the HTTP API; a **worker** is the per-session data-plane process that runs the harness. **Jev** is the hosted turn classifier the worker consults to decide whether a turn is finished, awaiting input, or quota-blocked; it needs the network.

## What is in scope

The full surface, as inventoried on 2026-09-23: the terminal dashboard (panes, Browse pane, dialogs, wizards, Setup, help, palette, composer and slash commands, chat controls), the web viewer (login, cards, new/resume/move, conversation, history paging, turn control, quota and targets pages), the CLI (`mj new/prompt/wait/transcript/diff/export/sessions/suspend/destroy/resume/interrupt-turn/put-file/usage/events/elicitations/respond/api-info/models/set-config/doctor/setup/import/recover/checkpoint/move/login/workspaces/go/app/daemon/acp`) and the bearer `/api/v1` API, daemon lifecycle and automatic upgrade from older releases, SSH targets (session sharding, per-instance control sockets, move, suspend/resume, checkpoint), a Docker target on a real host, the five real harnesses, harness-native and Mjolnir-managed sub-agents, turn review and continuation, quota display, session import from native transcripts, and the public install path and documentation.

The recent changes each track must probe by name are listed on its card with commit hashes. The coordinator's classification of every commit since 09-19 is not repeated here; a worker who wants the reasoning can run `git show <hash>` — the bodies are descriptive.

## Prepare

Work from `/home/jonathan/Projects/mjolnir`. Every command here opens loopback or Unix sockets, so run outside any restricted sandbox with elevated permissions; an `EPERM` from a sandboxed command is not a product result. Set these in every shell you use and record them at the top of your `notes.md`:

    export MJ_CAMPAIGN=/home/jonathan/mj-campaign/launch-2026-09-23
    export MJ_RELIABILITY_ARTIFACTS=$MJ_CAMPAIGN/evidence
    git rev-parse HEAD
    sha256sum $MJ_CAMPAIGN/bin/debug/mj $MJ_CAMPAIGN/bin/debug/mj-worker $MJ_CAMPAIGN/bin/x86_64-unknown-linux-musl/debug/mj-worker

Never read, write, or create anything under `target/`; if a command of yours would (for example a script that defaults there), stop and record it as a runbook error.

The user's own Mjolnir runs on this host on the **default instance** with a live daemon (`~/.local/share/mjolnir/daemon.json`) and real sessions. Nothing in this campaign may touch it. Every track isolates itself in one of two ways, named on its card:

**A fake-ACP lab** (tracks A–H, K). Prepare it with your assigned seed (with `MJ_RELIABILITY_ARTIFACTS` exported, which puts the artifact directory under the campaign root):

    python3 tests/e2e/prepare-luna-lab.py --seed <SEED> --hel $MJ_CAMPAIGN/bin/debug/mj

It prints `artifacts=<dir>` (under `$MJ_CAMPAIGN/evidence/`), `runtime=<dir>`, and a `source <file>` line. The environment file exports `MJ_CONFIG_DIR` and `MJ_DATA_DIR` (disposable config and state), `MJ_CHAOS_ISOLATED=1`, `MJ_LUNA_ARTIFACTS` (your lab's root, where all your evidence goes), `MJ_LUNA_BINARY`, `MJ_LUNA_PORT`, and `MJ_LUNA_FAKE_ACP_DELAY_MS`. The fake answers prompts after that delay, 15 seconds by default; that window is how you see a session in the *working* state. A card that needs many fast turns says so and may prepare a second lab with `--fake-delay-ms 1000`.

**A named instance** (tracks I1, I2, J). `MJ_INSTANCE=launch-<track>` (or `mj --instance launch-<track>`) puts config and data under `~/.config/mjolnir/instances/launch-<track>/` and `~/.local/share/mjolnir/instances/launch-<track>/`, both empty at first. The daemon, database, logs, and SSH control sockets are all per instance. Create the config directory, write a `config.toml` as your card says, and export `MJ_INSTANCE` in every shell you use. The config must begin with `version = 12`, and it must set `[phone] enabled = true` and `bind = "127.0.0.1:<port>"` with the port from your card — without it the instance tries the default viewer port, which the user's live daemon holds, and every CLI command fails with "Port 3765 is already in use" (finding I1-2 from the first run). Evidence for instance tracks goes under `$MJ_CAMPAIGN/evidence/<track>/`; create it first. Never run `mj recover … --all-instances`, and never run any `mj` command without `MJ_INSTANCE` set in that shell. `mj` is always `$MJ_CAMPAIGN/bin/debug/mj`.

**Real accounts.** Tracks I1, I2 and J use the user's real profiles. Copy only the `[profiles.claude]`, `[profiles.codex]`, `[profiles.muse]`, `[profiles.grok]`, `[profiles.kimi]` blocks from `~/.config/mjolnir/config.toml` into your instance config, verbatim, nothing else (where a card says `[subagents.eligible_profiles]`, write `claude = true` under it rather than copying the user's list, which names profiles you do not have) — the other profiles carry API keys for third-party routers and are not needed. Never copy a config file, token, cookie, or login URL into evidence; captures that show a login code must have the code redacted. Keep prompts short: one sentence, no long agentic tasks except where a mission needs a background task, and stop a session as soon as its mission is done. Every real session's project directory is a scratch repository you create under your evidence root (`git init`, three small commits), never one of the user's projects.

**tmux.** Start a private server named for your track, with a dashboard window sized 140×40 and a second window as your shell. The server name must be unique to you and must never be the user's own server:

    tmux -L mjl-<track> -f /dev/null new-session -d -s v -n mj -x 140 -y 40
    tmux -L mjl-<track> new-window -d -t v -n shell
    tmux -L mjl-<track> send-keys -t v:mj "source '<artifacts>/luna-env.sh'; exec \"\$MJ_LUNA_BINARY\"" Enter

(Instance tracks replace the `source` with `export MJ_INSTANCE=launch-<track>; exec $MJ_CAMPAIGN/bin/debug/mj`.) Drive it only through `tmux -L mjl-<track> send-keys -t v:mj ...` and read it only through `tmux -L mjl-<track> capture-pane -t v:mj -p`. `send-keys C-b g` reaches `mj` as the prefix chord even though tmux itself uses `ctrl+b`; because `mj` runs inside tmux with tmux's default prefix, the dashboard is entitled to show its inside-tmux collision notice on first launch. Send text with `send-keys -l 'text'` and Enter as a separate `send-keys Enter`. Leave about half a second between `C-b` and the second key; `send-keys ';'` never arrives (tmux eats it), so send `prefix+;` as `send-keys -H 3b`. A bracketed paste is `send-keys -H 1b 5b 32 30 30 7e` … `send-keys -H 1b 5b 32 30 31 7e` around the text bytes; an empty bracketed paste is the two sequences back to back. After any key that changes the screen, wait briefly (`sleep 0.3`) before capturing; after a prompt, wait for the fake delay. `prefix+q` exits the process rather than detaching, so the window that `exec`ed the dashboard closes with it. Put helper scripts under your own evidence directory, never in a shared path.

**Sessions in a fake lab.** The dashboard opens on an empty workspace. Create sessions with `n` in the Sessions pane (or `prefix+c`) and expect **four** steps — profile (`fake`), target (`localhost`), project directory (the lab's `project` path printed by the preparer), and a review — each accepted with Enter; the wizard never asks for a title, so rename afterwards with `prefix+shift+t`. The project-directory field remembers the last path, so clear it before typing. `prefix+shift+n` opens the workspace manager; `prefix+n`/`prefix+p` move between workspace tabs; `prefix+1`..`9` jump. Send a prompt by focusing the composer (`Tab` until the Prompt box has focus), typing with `send-keys -l`, then Enter. Session states: *working* is the delay window after a prompt; *done (unread)* is a reply that landed while you were elsewhere; *idle* is a read reply; *failed* is reachable only by killing the session's fake adapter or worker by exact PID (find it by `MJ_DATA_DIR` in `/proc/<pid>/environ`), after which the controller restarts the worker and the warning counts as unread — that is expected, and it is the only failure the lab can produce; *blocked / awaiting input* is **not reachable** with the fake (real-harness tracks cover it).

**Timing lessons kept from 09-19.** A state filter that hides the selected session also acts on the open conversation, so test filters from a session that is not the one open. Chords dropped at 0.2 s are harness timing, not a product bug. Two workers sharing one helper path overwrote each other's captures.

**Corrections from the first run of this campaign** (2026-09-23, 13:00–13:31). The session row menu (`⋯`) opens **only by mouse click**; there is no key and the footer names none (filed as A-8). Reach the same actions through the palette's session group — **Pin session…**, **Unpin session**, **Suspend session…**, **Restart session**, **Destroy session…**, **Rename…**, **Move…**, **Changed files** — or the chords. `+` and `*` are not pin keys; they are the ASCII glyphs of `◇`/`◆`. The Sub-agents pane likewise opened only by mouse in the first run; look for a keyboard path (palette or chord) and file a finding if there is none. Setup has no separate Theme section: theme is under Interface. `MJ_JEV_DISABLE` does not exist; to take the network away from the help search, restart the dashboard with `HTTPS_PROXY=http://127.0.0.1:9` in its environment. The `prefix+g` Import tab lists only the native homes of **configured** profiles, so a fake lab shows none; for the Import missions add `[profiles.nativeclaude] kind = "claude" home = "/home/jonathan/.claude"` to the lab's `config.toml` for the duration of those missions, never create a session with it, and remove it afterwards (it also makes the Quota pane read the real Claude quota, which is expected). Old releases ask "Upgrade now? [Y/n]" on launch: answer `n`. `mj doctor` exits 1 on an instance with no bundle configured (I1-1); that is not a sign-in failure — read the JSON.

## Evidence standard

Write everything beneath your evidence root (`$MJ_LUNA_ARTIFACTS/<track>/` for lab tracks; `$MJ_CAMPAIGN/evidence/<track>/` for instance tracks). Keep a `notes.md` that records, per mission, the wall-clock time, every key sent as the exact `send-keys` argument (or the exact command line, or the exact Playwright action), what you expected, what you saw, and the capture file name. Save terminal captures with `capture-pane -p > NNN-<label>.txt`, numbered in order; save browser evidence as accessibility snapshots and console/network summaries, with a screenshot only for an inherently visual question. Capture before and after every assertion, not only on failure.

A **finding** is a numbered entry `<track>-<n>` in `findings.md` with: a one-line title; the exact reproduction from a fresh dashboard, shell, or browser; the capture that shows the wrong state and the one just before it; what the correct behavior is and which commit, document, or help text promises it; and a severity — *blocks* (a person cannot complete the task), *wrong* (the task completes but the screen lies or a key does the wrong thing), or *rough* (works, but a label, hint, alignment, or timing is off). Add a fourth kind for this campaign: *docs* (the product is right but the documentation, help overlay, `--help` text, or first-run guidance says otherwise, or says nothing where a new user needs it). Record a finding before doing anything else, and do not attempt a fix or a workaround; if the dashboard is wedged, note it, restart it, and continue.

When a mission passes, say so in `notes.md` with the capture that proves it. "It seemed fine" is not a result. Finish `notes.md` with a table of missions and outcomes (`pass`, `finding <id>`, `not exercised: <reason>`), and end the run with the cleanup below.

## Mission cards

Each card is one worker's whole assignment. The **Recent** paragraph names the changes since 09-19 that the card must reach by hash; the **Surface** paragraph is the tour a first-time user would take through the same area. Do the Recent probes first — they are the likeliest to fail, and the coordinator starts fixes while tours are still running — then the tour.

### Track A — keyboard, discovery, help, and appearance (fake lab, seed 3101, server `mjl-a`)

Recent. `?` opens help with the filter already focused, typing filters at once, and `Esc` on an empty filter closes the overlay (c10eefa4); the help is grouped by task and, after about 400 ms, adds Jev semantic matches — type `move pane` and confirm substring matches appear immediately and a second, related group arrives after the pause; then set `MJ_JEV_DISABLE=1` or block the network if you can and confirm only substring matching remains (64ee22a7, 7372cef2). Open Setup (`prefix+s`) → Interface and confirm the prefix key is editable there (789b93cc); change it to `ctrl+]`, save, and confirm `C-]` then `g` opens the session dialog (a2f55d57 — tmux sends the legacy byte for `ctrl+]`); change it back. Confirm the Jev decisions inspector is gone: no "Jev decisions" entry in the session row menu or the palette (dc596b44).

Surface. Start the dashboard and capture the first screen (first-launch hint, inside-tmux collision notice). Open help with `prefix+?` and walk every group with `PageDown`, capturing each page; for every chord the help lists, press it once from a sensible state and confirm it does what the help says (this is the keyboard census — record a *docs* finding for any chord that is listed but dead, or bound but unlisted). Confirm `prefix+y` (unbound) shows the "not bound" notice and a doubled prefix sends the literal key to the composer. Open the palette (`prefix+:`), type `cre`, confirm **Create session** is first; run it, reopen the palette empty, confirm a **Recent** group. Read the footer for each pane focus and confirm each named key works; narrow to 100 and 80 columns and confirm hints drop from the right and `: palette` / `? keys` survive. Confirm `Esc` never quits and `prefix+q` exits. Restart with `NO_COLOR=1` and confirm the Monochrome theme; set ASCII symbols in Setup → Advanced (or `[advanced] symbols` in the lab's `config.toml`) and confirm borders, status marks, the footer joiner, and the Targets `% CPU · % RAM` header are plain ASCII (the middle dot was a 09-19 leftover). Resize to 79, 60, and 59 columns and confirm the stacked layout, the floor, and the "too small" message.

### Track B — Sessions pane, attention, and session lifecycle (fake lab, seed 3102, server `mjl-b`)

Recent. Open a session row's menu — it is mouse-only (A-8), and tmux can click: `tmux -L mjl-b send-keys -t v:mj -M` does not help, but a mouse-mode click is deliverable with the SGR sequence `send-keys -H 1b 5b 3c 30 3b <col> 3b <row> 4d` (press) then the same with `6d` (release), column and row as ASCII digits at the `⋯` glyph's position, if the dashboard has mouse capture on; if that fails, open the equivalent commands from the palette and record the menu missions as "via palette". Confirm the menu (or the palette's session group) is titled with the session name, lists each action once in task order (Changed files, Rename, Pin/Unpin, Container settings, Move, Suspend, Restart, Destroy), and shows impossible actions disabled rather than hidden (1befabde). Suspend an **idle** session from the menu or palette and confirm there is **no** confirmation; suspend a **working** one and confirm there is (421a106d). Suspend four sessions in quick succession and confirm each reaches the suspended state (f8d747fd). `prefix+shift+x` on a session asks to confirm, then stops it; `prefix+shift+w` renames the workspace; `prefix+shift+d` asks to confirm and closes the workspace, stopping its sessions and discarding drafts — cancel each once, then confirm each, then open `prefix+g` and confirm the closed sessions are still offered on the Mjolnir tab (981532a2). Kill one session's worker by exact PID: confirm the attention badge shows the most urgent unseen level and its count, that opening the failed session acknowledges it and the badge count drops, and that the count returns only if the failure changes (a6bf3801, c2d2f71a). Confirm "Interrupt turn", "Suspend session", "Destroy session", and "Close pane" are the words used everywhere the pane offers them (2e3077d9).

Two probes carried from 09-19 with no reproduction yet — try each three times, with captures each time, and record the exact sequence if either recurs: (1) `Tab`/`Shift-Tab` refusing to leave the Quota pane until `Esc`; (2) the Sessions pane ignoring `Up`/`Down`/`j` while `prefix+v` does nothing, until `/` then `Esc`.

Surface. Two workspaces, two sessions each, titled. Attention: send a prompt in one, switch workspace before it lands, confirm the tab badge and the terminal title (`tmux display -p '#{pane_title}'`); `prefix+o` and `prefix+shift+o` route to and from it. Filters: `/` search, then `a`/`b`/`w`/`i`/`d` state letters with the title reporting the state and `N hidden`, from a session that is not the one open. Digits `1`–`9` fold projects. The changed-files overlay (`prefix+d`) and the branch marker on a session's title line. The **Recent messages** palette command lists notices newest first. Rename, pin, and unpin a session from its menu.

### Track C — session dialog, wizards, Setup, and import (fake lab, seed 3103, server `mjl-c`)

Recent. In the New wizard the focused recent project is highlighted (7a6025b3); `ctrl+space` completion works on the path field. Setup checkboxes use one glyph family (`☑`/`☐`, or `[x]`/`[ ]` in ASCII) — walk every checkbox in every Setup section and record any other glyph (96d1cbe1). Setup has a **Continuation** section with "Automatically continue unfinished requests" on by default (0e3f98ff) and a **Review** section that offers **Auto** among reviewer choices (4502dbd7): toggle and save each, reopen, confirm persistence. On the `prefix+g` dialog's **Import** tab (which scans this host's native Claude and Codex homes, so real rows appear even in the lab): confirm each unavailable row explains why ("missing Git repo", "could not inspect…") and that the copy-session-id action reports "Copied session ID" (47f6d61c). Import **one** session whose row is available and whose project is a Git repository; confirm it appears stopped on the Mjolnir tab; do not prompt it. Confirm the Mjolnir tab never shows a sub-agent row (62ba3c1b — the lab has none, so this is a negative check: no row whose title or column marks it as a child).

Surface. `prefix+g`: Live tab focused on open, WORKSPACE column, state letters narrow the Live list, `/` search works before indexing finishes, Enter jumps across workspaces, `←`/`→` reach Mjolnir, Import and Archived with PROFILE/TARGET columns, Delete refused on Live. Resume a stopped session through the Mjolnir tab and complete the wizard. Every Setup section in order — Interface, Notifications, Theme, Web Access, Review, Continuation, SessionWiki, Sub-agents, Build cache, Agent Profiles, Machines, Runtimes (targets), Projects — opened, one field changed, saved, reopened; `Esc back` on a sub-page keeps the draft; the breadcrumb shows the user's names. Add a machine and a target through Setup (an `ssh` machine to a host that does not exist is fine for form validation; record what the validation says). The build-cache section: refilled keys keep their order (a 09-19 leftover). The changed-files, container, and dialog path fields' completion popup should stay inside the dialog (a 09-19 leftover).

### Track D — conversation panes, Browse, pins, and layout (fake lab, seed 3104, server `mjl-d`)

Recent. The **Browse** pane: with four sessions, moving the Sessions selection with arrows previews the selected unpinned session in the Browse slot without opening a new pane; pin with the palette's **Pin session…** (the `◇` control is pointer-only; `+`/`*` are its ASCII glyphs, not keys) and confirm the pin badge colour and the `◆` mark; unpin with **Unpin session** (2663c6d2). "Pin and browse right", "Pin and browse down", and "Pin in empty pane…" from the row menu each place the session where they say. `prefix+v` and `prefix+-` split the **targeted** pane; `prefix+h/j/k/l` focus; `prefix+shift+h/j/k/l` swap; `prefix+x` closes a view; `prefix+z` zooms; `prefix+r` enters resize mode where `h/j/k/l` or arrows resize and `Esc` leaves (981532a2). Build a three-pane layout, exit with `prefix+q`, restart, and confirm the layout returns for that workspace. The title-row close chip is pointer-only: confirm it is drawn; record wheel scrolling and chip clicks as not exercised.

Surface. `prefix+;` last pane. Splitting a session that is already visible shows the explaining notice. `PageUp`/`PageDown` scroll the focused conversation. Narrow layouts at 79 and 60 columns with panes open. Ten sessions across two workspaces to see how the Sessions list and pane slots cope. Detach and reattach the dashboard with sessions working and confirm the panes' contents recover.

### Track E — composer, chat controls, and transcript (fake lab, seed 3105, server `mjl-e`)

Recent. A submitted prompt and a `!ls` shell command each appear as a row in the conversation on the next render, with **no** "delivered" toast; the shell command's output arrives as a row (71ff0559, f92350f3 — also time Enter-to-row with a timestamped capture loop). Queue a second prompt while a turn is working and confirm a blank line separates the queue from the input (bd2cbdde); press `Esc` and confirm it asks to **steer** rather than silently cancelling, that pending delivery stays visible, that a second `Esc` does nothing, and that when the fake cannot steer the dialog offers cancel (5ea0d43d). Select a session and start typing immediately while the composer is still attaching; confirm nothing is lost (1a6e0adf). Paste: a bracketed paste of text lands as text; an **empty** bracketed paste against the fake (which advertises no image support) shows the "images not supported" notice and leaves the draft intact (e0e9dd83). History paging: prepare a second lab with `--fake-delay-ms 1000`, send 40 prompts, then `Ctrl+PageUp` opens the earlier-history view, Enter loads an earlier page, `Esc` returns with the draft kept (522fa6dc). `/clear` while the fake is working is refused; `/model` and `/effort` with the fake (which advertises none) are refused with a clear message; `/help` lists the commands; `/review status` answers; `/attach <path>` with a real text file; `/detach` (29a80b2d, and the 09-19 leftover about `/model` with an unlisted value).

Surface. A 70,000-byte prompt (under the 65,536-character limit, over 64 KiB) round-trips. Transcript scrolling to the top and bottom of a long conversation. `Esc` during a working turn cancels and the fake's log shows the cancel. The conversation's title line (model/effort). The `Sub-agents` entry in the prompt's lower border opens the sub-agent workspace and its `X` returns (empty in the lab). Dictation toggle: press it and record what the dashboard says without a microphone (it must not wedge).

### Track F — CLI and HTTP API (fake lab, seed 3106, mostly no tmux; server `mjl-f` for dashboard checks)

Recent. `mj new` with no `--profile`/`--target` uses the saved default pair (6eaa8aa6; set the default first through one dashboard-created session, or `mj set-config`); `mj new --base HEAD~2 --project-directory <lab project> --json` starts at that revision; in the session's worktree check out an older commit and confirm plain `mj diff` warns on stderr that HEAD diverged and `--json` reports `head_descends_from_base: false` (b3a94159); `mj diff --base HEAD~1 --json` reports resolved base and head commits (7156b0d3). `mj close` is rejected and names its replacements; `mj suspend`, `mj destroy --session --delete-branch`, `mj resume`, `mj interrupt-turn` each work and `mj cancel-turn` is gone (2e3077d9). `mj api-info`, then `curl` `GET /api/v1/options` with the bearer token and confirm profiles, targets, bundles, availability, and the default pair (69802740). `mj acp`: pipe an `initialize` request and confirm the reply (df3c0f90); then write a small stdio client (Python) that sends `initialize`, `session/new` with the lab's project directory, and `session/prompt`, and confirm the session appears in `mj sessions --json` and the dashboard (ad327f64); send `session/cancel` mid-prompt and confirm the turn stops; kill the client mid-prompt and confirm the turn stops (2c9b6fb9). `GET /api/v1/sessions/{id}/history` pages backward (522fa6dc). `mj doctor --json` passes on the lab config; `mj doctor --json --smoke` runs the Podman smoke here (podman 5.7 is installed; record its result, and note the mbx warning wording if it appears — 4b134226).

Surface. Every remaining subcommand once, against a lab session: `prompt --wait`, `wait`, `transcript --after-seq`, `events` (stream 10 seconds), `usage`, `put-file`, `elicitations` (empty), `sessions`, `export --kind patch` and `--kind branch`, `checkpoint`, `models`, `set-config`, `workspaces list`/`create`, `recover scan` (**never** `--all-instances`), `setup instructions --platform linux` and `--platform macos` (read both as a new user; *docs* findings welcome), `daemon status`/`stop`/`restart` (the dashboard must survive `restart`), `go <folder>` on a scratch git repo, `import codex --latest --bundle …` on a real native transcript into the lab, and `mj app` (WSLg gives a display; record whether the desktop window opens and shows the viewer, or the exact error). `mj --help` and every `mj <sub> --help`: read them as a new user and file *docs* findings for anything unclear or stale. Compare `docs/src/content/docs/cli-reference.md` and `api-reference.md` against what the binary accepts.

### Track G — web viewer (fake lab, seed 3107, Playwright; server `mjl-g` for the paired dashboard)

Mechanics. Start the dashboard in tmux as usual and open the Web dialog (`prefix+w`, or the palette's "Web viewer") to get the login code and QR URL; `tests/e2e/browser_lab.py` (`qr_url`) and `tests/e2e/reliability_lab.py` show how a script can read them from the daemon if the dialog is awkward. Drive Chromium with Node scripts that `require('/home/jonathan/Projects/mjolnir/tests/e2e/web/node_modules/playwright')` — the pinned 1.62.1 — with `ignoreHTTPSErrors` and `--ignore-certificate-errors` (the lab serves a self-signed certificate). Use accessibility snapshots, roles, labels, console messages, and request status as evidence; screenshot only for layout questions. Redact the login code from any saved snapshot.

Recent. Log in with the code; bookmark the QR login URL, restart the daemon (`mj daemon restart` in the lab env), clear the browser context, open the bookmark, and confirm it still logs in (0b972454); log out and confirm reusing the old cookie is rejected (ba9c3075). New session: pick the profile and target, then confirm the **Review** step appears at once while the preflight runs and **Launch** is disabled until it passes (42b8bcf9). The session menu says Interrupt turn / Suspend session / Destroy session and the card says "suspended" after a suspend (2e3077d9). A prompt and a shell command appear as rows at once, no toast (71ff0559). After 40 fast turns (a second lab at `--fake-delay-ms 1000`), the conversation shows **Earlier messages** and **Load earlier page** works (522fa6dc). Native sub-agent rows do not appear in the resume list (62ba3c1b, negative check). During an upgrade handoff (coordinate with nothing — simulate with `mj daemon restart`) requests get 503 with `x-mj-upgrade: pending` and the page recovers without a reload (2226599f, b93d2610).

Surface. Every route: dashboard (cards, project groups, compact cards), new, resume, move, targets, quota, conversation. Desktop (1280×800) and mobile (390×844) viewports. Browser back/forward. Turn control interrupt. Plan-mode controls if the fake exposes modes. Attachments (a text file). Wiki search box. Offline → online SSE recovery (`context.setOffline`). Two clients (browser and the paired dashboard) converging on the same session state after a prompt, a rename, and a suspend. Console must show no first-party 404s or errors — record any.

### Track H — daemon lifecycle and automatic upgrade (fake lab, seed 3108, server `mjl-h`, plus older releases)

Mechanics. Download the older releases' Linux GNU archives with `gh release download vX.Y.Z -p '*x86_64-unknown-linux-gnu.tar.gz'` into your evidence root and extract each into its own directory; host glibc is 2.43, so they run. Verify the extracted `mj --version`. To run an **old** build on the lab: `source luna-env.sh; exec <old>/mj` in the tmux window; the lab's `config.toml` is the current schema version, so if an old build refuses it, record the exact message (that is itself a finding about downgrade protection) and try the next-older path only if the card needs it. To **upgrade**, leave the old daemon running with sessions and start the campaign `mj` in the same lab env in a second tmux window.

Recent. From **2.15.0**: create two sessions, one mid-turn (use a lab with `--fake-delay-ms 60000` for this), then run the campaign `mj`; confirm the dashboard reports upgrade progress, the daemon is replaced without a manual restart, the database migrates, both sessions survive, and the mid-turn reply lands afterwards (2cfb84ef, 2226599f, 0bfc1833). From **2.17.0**: create a session, kill its worker by exact PID so the daemon's worker-bound gate would wait forever, then run the campaign `mj` and confirm the daemon is replaced at once rather than waiting on "ongoing work" (e3b8494f). From **2.19.0**: start `mj wait --session <id> --timeout 600` (an open HTTP request) in the shell window, then upgrade; confirm the upgrade completes while the request is open and only new requests are refused during the gate (b93d2610). After an upgrade from 2.15.0 with a session mid-turn, press `Esc` in that chat and confirm the **old** worker's turn is interrupted (8b5ecb51). `mj daemon restart` on the campaign build with the dashboard attached: the dashboard reconnects, workers keep running. `mj daemon stop` then a dashboard start: the daemon comes back.

Surface. The CLI wait notice during an upgrade names what it is waiting for: start a suspend on a slow session and upgrade at once. Two dashboards attached to one lab daemon during an upgrade both recover. Worker upgrade: after the daemon upgrade, confirm each session's worker is replaced when idle (worker PID changes, session survives) and is **not** replaced mid-turn (31ecea97 — the Codex never-prompted case needs a real account and is on I2's card).

### Track I1 — real Claude: sub-agents, turn boundaries, memory tools, review (instance `launch-i1`, server `mjl-i1`)

Setup. Instance config (`version = 12`; `[phone] enabled = true`, `bind = "127.0.0.1:3771"`) with `[profiles.claude]` copied verbatim from the user's config, `[subagents.eligible_profiles] claude = true`, and `[targets.localhost] kind = "bare"`. Scratch git repo with three commits and a few Rust or Python files. Confirm `mj --instance launch-i1 doctor --json` shows the Claude profile signed in before starting.

Recent. Select Opus 5.5 with `/model claude-opus-5-5` (or the profile picker) and confirm it is accepted and the title shows it (44957c9f); also `/model not-a-model` — the 09-19 leftover says this still reports "accepted"; record the current behavior. Prompt: "Use the Task tool to start two subagents that each list the files in this repository, then summarize" — confirm both appear in the **Sub-agents** pane with name, task, lifecycle, and a live transcript, and that selecting one shows it (039b0536, 7f6948f6); suspend the session and confirm `prefix+g` → Mjolnir shows one row and none for the children, and that a phrase from a child's transcript is not found by resume search (62ba3c1b). Prompt: "Run `sleep 90` as a background task and tell me now that you started it; when it finishes, tell me the result" — confirm the session goes idle after the first answer despite the background task (b9bba65d, 65a73905), and that when Claude resumes on its own after the sleep the session shows working and ends at its result without Stop (6b6e684c); repeat and press `Esc` during the self-started cycle and confirm it interrupts (4c379aca). Prompt something that makes Claude ask a question (e.g. "Before you do anything, ask me which of two file names to use") — confirm the session shows awaiting input, that the form keeps its label and shows validation in the border when the pane is shrunk below 6 rows (d6df0ef1), and that a parent asking while a child works becomes awaiting input within about a minute (66849553 — combine with a Task subagent). `/clear` while idle: a lasting divider, the model no longer recalls the earlier context, files and transcript stay; `/clear` while working is refused; `/compact` on the longer session (29a80b2d). Memory tools: "Use the mj-memory tools: search_sessions for 'sleep', then blame_file on <a file>" — confirm the tool calls succeed on localhost (65fabd07); suspend, resume, and ask it to list its mj-memory tools (f06a95ce). Review: Settings → Review set to **Auto** with only Claude configured, finish a turn, and confirm review runs in a separate conversation and reports (4502dbd7); toggle continuation off and on. Continuation: "Add a function `double(x)` to <file> and run the tests" — if Claude stops to ask, confirm "Checking continuation" appears and a continuation prompt follows (0e3f98ff); type during the check and confirm it cancels. Mjolnir-managed sub-agents: ask Claude to use `spawn_agent` (the mj sub-agent MCP tool) with an eligible profile to "count the lines in README", then `wait_agents`; confirm the child appears in the sub-agent workspace and the result reaches the parent. Quota pane: capture it and record the reset countdown format (1d57913e).

Surface. New wizard end to end with a real profile (model and effort selectors present). Interrupt a working turn with `Esc`. Rename, pin, suspend, resume, destroy (with `--delete-branch` from the CLI). Notifications on a reply landing while another session is open. Import tab: the real Claude transcript of this very session should appear once the session is stopped.

### Track I2 — real Codex, Muse, Grok, Kimi: harness smoke and Codex specifics (instance `launch-i2`, server `mjl-i2`)

Setup as I1 (`bind = "127.0.0.1:3772"`) with `[profiles.codex]`, `[profiles.muse]`, `[profiles.grok]`, `[profiles.kimi]`. `mj doctor --json` first; record which are signed in (Kimi may not be — then record it as not exercised, and note whether the doctor's wording would tell a new user what to do).

Recent. Codex: one prompt round-trips (8ccbad10, 3be3cb15); `/fast` toggles; `/clear` and `/compact` as on I1 (29a80b2d); memory tools include the document tools (`trace_file`, `session_files`) (65fabd07); ask Codex to spawn its native agents ("use your agents to review two files in parallel") and confirm they appear in the Sub-agents pane (039b0536). The never-prompted upgrade case: create a Codex session and send **nothing**; suspend it; resume it and confirm it reopens (31ecea97 — the full case needs an older build and is recorded as partially exercised). Muse: one prompt; make Muse ask a multiple-choice question and confirm a **single** form appears (the route form is answered automatically) and `Esc` mid-turn interrupts (cb3d8afe). Grok: one prompt after on-demand install; record install time and any notice (6d18d5a1). Kimi: one prompt if signed in. `mj move --session <codex id> --profile claude`? — not on this card (needs both profiles); instead move between two Codex sessions' targets is not applicable on localhost; record as not exercised.

Surface. For each signed-in harness: New wizard (note which selectors it offers), one prompt, `Esc` interrupt, suspend, resume, destroy. Quota pane rows for each. `mj usage` and `mj transcript` for one Codex session. Import tab shows the native Codex transcript after stop.

### Track J — EC2: fresh public install, SSH-bare sharding and move, SSH-Docker target (instance `launch-j`, lab seed 3110 for the fake, server `mjl-j`)

Provision. One Ubuntu 24.04 `t3.large` in `us-east-1` through the repo's tool, which tags, ledgers, and expires the host after four hours:

    python3 tests/e2e/ssh_docker_lab.py create --artifact-dir $MJ_CAMPAIGN/evidence/track-j/ec2
    python3 tests/e2e/ssh_docker_lab.py status --artifact-dir <same>

Read the ledger for the address, key, and user (`ubuntu`). The ledger is the only copy of the private key and the resource ids; the tool's `cleanup` needs it, so back it up to a second directory under your evidence root right after `create`. (The first run lost the ledger and cleaned up by the run tag `mj-ssh-docker-run` with the AWS CLI; that path works and is recorded as J-9.) The instance configs use `bind = "127.0.0.1:3773"` for `launch-j` and `3774` for `launch-j2`. Add a Host entry to a **campaign-only** ssh config file under your evidence root (never `~/.ssh/config`) and use `ssh -F` everywhere; the `[machines.<name>] kind = "ssh"` entry in your instance config can carry `ssh_args = ["-F", "<that file>"]`. Cost ceiling: one host, under four hours; run `ssh_docker_lab.py cleanup` at the end and confirm the ledger reports every resource gone.

Fresh install (do this **on the host**, as the `ubuntu` user, exactly as a new user would, reading only `README.md` and `docs/src/content/docs/{quickstart,install}.md`). Run the README's `curl … install.sh | bash`. Then follow the quickstart word for word: `mj setup`, `mj doctor`, first `mj` launch. Record every point where the docs and the product disagree, every message that assumes knowledge a new user lacks, and what the product says when **no** coding agent is installed (there is none on the host). Then install nothing real: copy the lab's fake fixture directory to the same absolute path on the host, write the fake profile into the host's `mj` config, and create one session end to end from the host's own dashboard (in a tmux there). This proves the released installer and the first-run path; file *docs* findings freely — the user asked for the first-run experience to be read critically.

SSH-bare from the controller (needs the portable worker, which the campaign built). Instance config: the `ssh` machine above, `[targets.ec2] kind = "bare"` on it, and the lab's `[profiles.fake]` block (the fake's PATH must exist on the host — you copied it in the previous step). Create **twelve** sessions on `ec2` from the dashboard or `mj new --target ec2` and confirm all reach idle with a reply; stock `sshd` allows 10 sessions per connection, so this proves sharding at 8 per master (59e249d5). List `$XDG_RUNTIME_DIR/mjolnir/launch-j/` and confirm the per-shard socket names; start a second instance (`launch-j2`) against the same host and confirm its sockets are separate (c4f88121). Kill one control master by PID and confirm sessions recover through a re-opened master, not a direct connection (watch `ps` for ssh processes without `-S`/ControlPath). Set `ssh_args = ["-o","ControlPath=<your path>"]` on the machine and confirm Mjolnir adds no sharing options of its own. Then: suspend one session and resume it; `mj checkpoint`; `mj move --session <id> --target localhost` and back to `ec2`; `mj export --kind branch`; destroy one and confirm the host's worker directory for it is gone.

SSH-Docker from the controller. Add `[targets.ec2-docker] kind = "docker"` on the same machine with the `ghcr.io/brokkai/mjolnir/agent-dev:latest` image, and the user's `[profiles.codex3]` block copied verbatim (this is the profile the repo's own `ssh_docker_acceptance.py` uses; `mj` delivers its credentials to the container itself, as it does daily for the user's Podman host). Create one Codex session on it, send one short prompt, confirm the reply. Open **Container settings** from the row menu and change a mount; confirm the validation against the running session. `mj doctor --json` on the instance: record the mbx compatibility warning wording if the host's mbx is older (4b134226). Remove the `[targets.ec2-docker]` block from the instance config, restart the daemon, and confirm `mj export --kind patch` and `mj destroy` on that session **still work** (d81fd67c). Finally, confirm no containers remain on the host (`docker ps -a` over ssh) and run the cleanup.

### Track K — documentation against the product (fake lab, seed 3109, server `mjl-k`)

Read every page under `docs/src/content/docs/` and `README.md` as a person deciding whether to install, then as a person who just did. For each documented command, flag, key, setting, file path, and behavior, check it against the campaign binary: `mj <sub> --help`, the help overlay, Setup's sections, the lab's `config.toml`, and a live dashboard. Record each mismatch as a *docs* finding with the page, the sentence, and the capture or `--help` output that contradicts it. Also record the absences: what a new user needs on their first hour that no page says (how to get out, what a target is, what happens to their repo, what costs money). Check `docs/src/content/docs/terminal-surface.md` lists the Browse pane, pins, resize mode, and the `prefix+shift+x/d/w` chords (981532a2, 2663c6d2), `cli-reference.md` lists `suspend/destroy/interrupt-turn` and not `close/cancel-turn` (2e3077d9), `acp-agent.md` matches `mj acp` (694a0a1d), and `api-reference.md` lists `/options` and `/history` (69802740, 522fa6dc). Read `mj setup instructions --platform macos` too, for Track M.

### Track M — macOS (GitHub issue; the user drives it on a MacBook Air)

The coordinator files this card as an issue rather than running it. It covers what only macOS can show: the Homebrew and `install.sh` paths on macOS; `mj setup instructions --platform macos`; the macOS Claude quota reader (`mj-controller/src/claude_usage.rs`); keychain-backed credentials; `Cmd+V` image paste in Terminal.app, iTerm2, and kitty; system notifications; the `ctrl+]` prefix in each terminal; the `apple-container` target end to end; `mj app` (the desktop shell) including its cookie expiry after logout (ba9c3075); the daemon upgrade from the previous Homebrew release; and `mj doctor --smoke` with Docker Desktop or Podman machine. The issue text carries the evidence standard above and the same finding format.

## Cleanup (every worker, at the end)

Exit the dashboard with `prefix+q`, then kill your private tmux server: `tmux -L mjl-<track> kill-server`. Find the lab's or instance's processes by exact environment, never by name: the controller daemon runs as `/proc/self/exe daemon-run`, so an argument grep misses it — its PID is in `$MJ_DATA_DIR/daemon.json` (or the instance's data dir), and the reliable test for any process is whether `/proc/<pid>/environ` contains your `MJ_DATA_DIR` or `MJ_INSTANCE=launch-<track>`. Prefer `mj daemon stop` in your env first; then kill the daemon by PID, then the workers; a worker killed while its daemon lives is restarted. `daemon stop` deliberately leaves workers, supervisors, and fake adapters running (they are the data plane and survive daemon restarts), and those processes do **not** carry `MJ_DATA_DIR` in their environment — match your lab's runtime path (`/tmp/hel-r-<pid>-…`) in `/proc/<pid>/cmdline` as well, and expect twenty or more of them after a busy track. Kill by exact PID only; never `pkill` by name. Check again after a few seconds. Leave every artifact directory in place; the coordinator reads it. Do not delete anything under `$MJ_CAMPAIGN/evidence/`. Track J additionally runs the EC2 cleanup and saves its output. Instance tracks leave their instance directories in place (the coordinator removes them after triage).

## After the campaign (coordinator)

Read each track's `findings.md`. For each finding, decide whether it is a product defect, a documentation defect, a test-lab limitation, or a runbook error, and record that verdict beside it. File one GitHub issue per confirmed product or documentation defect (title, reproduction, severity, track and finding id, labels `bug` or `documentation` plus effort/impact), claim it with `agent-in-progress`, group by area, and dispatch one fix agent per group in an isolated worktree with the finding's exact reproduction, the requirement to write a failing test from that reproduction first, and the usual validation (`cargo test`, `cargo clippy --all-targets -- -D warnings`, dev profile, outside the sandbox). Commit messages close the issues. Cherry-pick in order; several agents editing one checkout clobber each other. Re-run only the missions the fixes touched, on fresh labs, before declaring the campaign closed. Record the outcome and the artifact directories at the bottom of this document.

## Run 1 of 2026-09-23 (13:00–13:31, binaries under `target/`; stopped)

All twelve tracks started at 13:00 against the binaries in `target/debug/`. At 13:31 the build cache (mbx, `[target] max_age = "1d"`) collected the managed directory behind `target/` — it was renamed `…removing-2269265` and deleted while an unrelated `late-mbx-cohort.py --execute` run and an `mbx nextest run` were active on the host. The three binaries, the downloaded old releases, the EC2 ledger and key, and every track's captures under `target/reliability-artifacts/` were lost. Every track stopped, cleaned up by exact PID, and rewrote its notes and findings from context into the coordinator's scratchpad (`scratchpad/track-<x>-recovered/`); Track J terminated the EC2 instance, security group, and key pair by their tagged ids. The user's own daemon on the default instance was also left running from a deleted `target/release/mj`.

What run 1 established before it stopped (captures lost; reproductions kept):

Passed: first-launch screen; `?` with the filter focused and grouped help with delayed related matches; offline text-only help; prefix editable in Setup and `ctrl+]` working inside tmux; the Jev inspector gone from the palette; unbound-chord notice; doubled prefix; palette ranking and Recent (A). Row menu title/order/disabled actions; idle suspend without confirmation; working suspend with confirmation; four quick suspends (B). `ctrl+space` completion inside the dialog; Continuation and Review sections present, toggles persisted; Import rows explain unavailability and copy the id; all thirteen Setup sections open (C). Browse pane preview; pin/unpin via palette; the three "Pin and browse" placements; split/focus/swap/close/zoom/resize; layout restored after restart; last-pane; already-pinned notice (D). Automatic upgrade from real 2.15.0 with a mid-turn session (daemon replaced 0.08 s after the old one stopped, both sessions survived, the reply landed), idle worker replaced within 3 s and the busy one only after its reply, and `Esc` interrupting the old worker's turn after the upgrade (H). Claude signed in; `/model not-a-model` now rejected; native Task sub-agents shown with live transcript; sub-agents kept out of the Mjolnir tab; idle after an answer with a background task running; mj-memory tools on localhost and after resume; countdown formats `1d 14h` / `4h 47m`; New wizard; suspend/resume (I1). EC2 provisioning, `install.sh` v2.20.0 on a fresh Ubuntu host (J).

Findings from run 1, pending triage (severity as filed by the worker): A-1 multi-word help query gets no text matches; A-2 stale help header (`[keys] in config.toml`, `/ search`); A-3 lowercase unavailability fragments in help rows; A-4 dictation chord gives no feedback; A-5 `Esc` in the workspace Rename/Close dialogs opens the Workspaces manager; A-6 Create session never in the palette's Recent; A-7 Cancel operation silent with nothing to cancel; A-8 row menu mouse-only; A-9 Setup continuation label runs into its truncated value. B-1 = A-8; B-2 conversation title shows the state cut from the left (`Conversation e`); B-3 confirm/rename dialogs name the session by id; B-4 a finishing launch steals the Sessions selection so `prefix+shift+x` acts on the wrong session. C-1 a project used this run is not offered in the next New wizard until restart; C-2 focused recent project drawn in plain colour (7a6025b3 not effective); C-3 switches mix `☑`/`☐` with On/Off text; C-4 Continuation description cut mid-sentence and used as the section name; C-5 Review Auto summary says "no reviewer" yet a review pane opened (uncertain); C-6 Enter on a Live search match left the dialog open (seen once); C-7 review step shows `Compute: ·` with nothing before the separator; C-8 import confirmation does not say whether the user's repository is modified. D-1 Browse title shows `e` for Idle; D-2 "Not enough room to split" notice persists after a later successful split; D-3 pinned rows lose their `⎇` marker after restart. I1-1 `doctor` exits 1 without a bundle; I1-2 **blocks**: a named instance's CLI fails on the default instance's viewer port; I1-3 `/model claude-opus-5-5` rejected while the picker's `opus[1m]` works; I1-4 rejection notice shows an internal `set-config-<hex>` id; I1-5 resume search matches the parent on a child-only phrase while the preview says no hits; I1-6 same-profile resume warns "Lossy: text only" and switches the model back to the default; I1-7 a new reply after resume does not scroll into view. J-1 doctor/setup fixes say "F7 Settings" (it is `prefix+s`); J-2 first launch with no agent installed never says what to install, and the quickstart's promised Codex profile is absent; J-3 `mj setup` says "Press n" after exiting; J-4 the setup smoke test is silent for ~2 min while pulling 1.93 GB; J-5 `doctor` tells the user to run `doctor --json` to see the fixes it just printed; J-6 Podman message cites `docs/PODMAN.md`, a repo path, twice; J-7 Targets pane lists `podman` where Podman is not installed; J-8 the quickstart never mentions `mj setup`; J-9 `ssh_docker_lab.py cleanup` cannot work without its ledger although resources are tagged. Also noted, unfiled: `search_sessions` in an instance returned default-instance sessions (host-wide memory index); a prompt typed during "Starting" went to the Sessions pane; Live state letters do not toggle off; the "Choose empty pane" menu draws over the Workspaces pane; 2.17.0's `mj new --workspace mjolnir` said `unknown workspace`.

## Run 2 of 2026-09-23 (binaries and evidence under the campaign root)

Started 13:40 against the same binaries (`f78ccc09` / `844e2f63` / `ec2550bf`), copied to `/home/jonathan/mj-campaign/launch-2026-09-23/bin/`. Evidence per track under `/home/jonathan/mj-campaign/launch-2026-09-23/evidence/` (`luna-manual-seed-<seed>-<pid>/track-<x>/` for lab tracks, `track-i1/`, `track-i2/`, `track-j/` for instance tracks). Five run-1 workers (E, F, G, I2, K) never progressed past their first steps — most likely a background permission prompt — and were stopped and relaunched fresh at 14:25. The harness refused most workers' `findings.md` writes ("subagents should return findings as text"); the coordinator wrote those files from the reports, and each says so at the top.

Results by track (captures are numbered in each evidence directory):

- **A** (seed 3201, 160 captures): A-1…A-9 re-confirmed; new A-10 pinned suspended session retried on every start (wrong), A-11 Quota footer "edit profile", A-12 composer footer drops the prefix label at ≤100 columns (wrong; the case 22d10cb4 was meant to close), A-13 hint over splash, A-14 NO_COLOR theme label, A-15 ASCII leftovers, A-16/A-18 help wording, A-17 no keyboard path to Sub-agents (blocks for keyboard users).
- **B** (seed 3202, 179 captures): B-1…B-4 re-confirmed; both 09-19 carried-over probes did not reproduce in three attempts each; the lab cannot reach *Failed* (a killed worker shows *Unreachable* and recovers — B-14), so a6bf3801's acknowledgement path stays with unit tests; new B-10 rename during Starting silently lost, B-11 = A-5, B-12 "waiting" for unreachable, B-13 raw log in the unreachable notice, B-15 `daemon stop` leaves workers without `MJ_DATA_DIR` in environ (runbook fixed), B-16 no "Interrupt turn" anywhere in the terminal, B-17 folding hides no rows (docs), B-18 notice-log time column.
- **C** (seed 3203, 212 captures): C-2…C-8 re-confirmed, C-1 not reproduced; new C-10…C-25, of which C-23 **blocks** (numeric Setup fields fail with `invalid type: string`), C-21 saving Setup deletes config comments, C-19/C-20/C-5/C-6 wrong; C-11 is by design (the two-cell modal halo, `blank-modal-halo.md`). The real import was not exercised (permission prompt).
- **D** (seed 3204, 73 captures): D-1…D-3 re-confirmed (D-3's cause is not pinning); new D-10 chooser at 0,0, D-11 stray letters in narrow titles, D-12 sequential `mj new` 429, D-13 wrong workspace and flipped tab order after restart, D-14 narrow pinned pane does not follow new replies (wrong).
- **E** (seeds 3205/3215, 72 captures): E-1 `!` shell commands fail (wrong), E-3 empty paste on WSL reads the clipboard before the capability check (wrong), E-5 unknown slash command sent as a prompt and a refused one stays in the draft, E-6 `/attach` wording, E-7 no Sub-agents border entry before children exist, E-2/E-4/E-8/E-9 duplicates. History paging needs ~160 fast prompts, not 40.
- **F** (seeds 3206/3206-752951, 156 CLI captures): F-1 `mj acp` ignores `--workspace` (**blocks** with two workspaces), F-2/F-3 ACP cancel and client disconnect do not stop the turn (wrong, contra 2c9b6fb9), F-4 new session refuses prompts for ~20 s, F-6 `mj wait` errors after a successful suspend, F-7 `/options` says docker ready, F-9 only `mj go` saves the default pair, F-10/F-11/F-12/F-13 CLI wording, F-16/F-17 `--help` and reference gaps, F-18 `mj import` reads the host's `~/.codex` not the profile home. `mj app` not exercised (no `mj-desktop` beside the campaign `mj`).
- **G** (seeds 3207/3217, 131 captures, Playwright): login, bookmarked QR URL across restart, logout revocation, preflight Review, history paging, offline recovery, two-client convergence all pass; G-1 CLI cannot reach the daemon once the viewer serves a self-signed certificate (**blocks** the Tailscale/phone setup), G-2 `mj new` sessions in `default` invisible in the viewer, G-3 401 console error on the login page, G-4 `ERR_ABORTED` on completed requests. The lab needs `phone_tls=True` for a QR URL; the Web dialog is `prefix+u`.
- **H** (seeds 3208…3258, real 2.15.0/2.17.0/2.19.0 releases): the 2.15.0 upgrade with a mid-turn session, worker pairing, `Esc` on the old worker, two dashboards through an upgrade, and downgrade protection all pass; H-1 the waiting `mj` spins at ~1900 % CPU (wrong), H-2 an upgrade from a 2.19/2.20 daemon stalls until every open HTTP request ends (wrong; b93d2610 is daemon-side), H-3 `mj new --workspace` vs the dashboard's directory-named workspace, H-4 stale "daemon unavailable" after `daemon restart`, H-5 raw `daemon.json` errors for a stopped daemon, H-6 internal checkpoint-barrier line in the transcript.
- **I1** (instance `launch-i1`, real Claude, 107 captures): I1-1…I1-7 re-confirmed except I1-2 (gone with `bind` set); self-started turns end and interrupt correctly, background-task idle, awaiting-input with a working child, `/compact`, review Auto, continuation (1 of 4 tries fired), managed sub-agents, memory tools all pass; new I1-10 = A-17, I1-11 `/clear` refused on an idle session (**blocks**), I1-12 refused commands pinned with duplicated internal text, I1-13 `/clear` after resume wedges on "Context is being cleared" (**blocks**), I1-14 wrong notice on a cancelled review, I1-15 review reports "no material findings" while every analysis failed (wrong), I1-16 LAST ACTIVE shows creation time, I1-17 no marker for an interrupted turn, I1-18 question row shows one word.
- **J** (instances `launch-j`/`launch-j2`, one EC2 `t3.large`, 11 min + ~70 min, cleaned up by id): install.sh, first run, fake session on the host, 12-session sharding on 2 shards, per-instance sockets, master recovery, user ControlPath, suspend/resume, checkpoint, destroy, target-removed export/destroy (d81fd67c) all pass; J-1…J-8 re-confirmed (J-2 changed: the Codex profile is created, but nothing says Codex is absent until a launch fails); new J-10…J-25, of which **J-19 (security)** — `StrictHostKeyChecking=accept-new` precedes the user's `extra_args` so strict checking cannot be turned on; J-17 sshd still refused 52 session opens under sharding; J-21 changing `extra_args` breaks resume; J-23 `mj wait` says finished during provisioning; J-11 doctor and dashboard disagree on docker; J-16 = D-12. `mj move` off SSH-bare is refused (J-22, docs). The SSH-Docker session reached the container but the `codex3` account was over quota (J-25).
- **K** (seed 3209, 92 captures + every `--help`): 18 findings — K-1/K-2 wrong (the docs promise a startup session and a shared-folder default), K-3…K-16 stale or missing docs, K-17 nothing tells the user that Jev sends prompt/reply text to a hosted classifier by default (and it cannot be turned off), K-18 first-hour gaps (stop/uninstall, worktree and branch creation, what costs quota or money, what a profile and target are).
- **I2**: (pending).

Runbook errors found in run 2 and corrected above or to correct: `ssh_args` is `extra_args`; the Web dialog is `prefix+u`; the lab viewer is HTTP-only unless `phone_tls` is set; history paging needs ~160 fast prompts; `mj set-config` is per session (only `mj go` saves the default pair); the managed sub-agent tools are `mcp__mj-agents__spawn`/`wait`; the Import tab reads native transcripts from each **profile's** home; `daemon stop` leaves workers that carry no `MJ_DATA_DIR`.

### Fix wave (in progress)

Fourteen fix agents in isolated worktrees, one per file-ownership group, each writing a failing test from the finding before fixing; cherry-picked onto master in order with the crate suites and workspace clippy run after each group. Landed so far (105 commits on master, `20e98203..8100bee6`): help/palette (A-1, A-2, A-3, A-6, A-7, A-9/C-4); wizards and Setup (C-1, C-2, C-3, C-7); panes and dialogs (B-4, A-5, B-2/D-1, B-3, D-2, D-3); controller and doctor (I1-2 per-instance viewer port, I1-1, J-1, J-3, J-4, J-5, J-6, J-7); Claude model/resume/search (I1-3, I1-4, I1-6, I1-7, I1-5); footer and key routes (A-12, A-17 `prefix+shift+a`, A-8 `prefix+.`, B-16 `prefix+i`, A-3 palette, A-11, A-16/A-18, A-14); session dialog (C-6, C-14, C-25, C-12, C-13); panes/workspaces/notices (A-10, D-13, D-11, D-10, A-13, A-15, B-10, B-12, B-13, B-18); daemon lifecycle (H-1, H-2, H-4, H-5, H-6; H-3 held for a design decision); Setup save/validation (C-23, C-21, C-19, C-5, C-22, C-24, C-17, C-20, C-18, C-15/C-16); documentation (K-1…K-18, J-2, J-8, C-8, F-15, F-17); `/clear`, review, Claude session (I1-11, I1-13, I1-15, I1-12, I1-16, I1-17, I1-18, I1-14); the EC2 tool's cleanup by tag (J-9). Root causes worth keeping: the pane label was drawn over the title row (B-2/D-1); 7a6025b3 read focus after `begin_frame` reset it (C-2); tabs were sorted by random workspace id (D-13); every 250 ms liveness probe refreshed one PID through `sysinfo`, which reads all of `/proc` on a rayon pool of one thread per CPU (H-1); `/clear` counted acknowledged-but-completed dispatches as work (I1-11); the quick review reached a clean result without checking the analysis had failed (I1-15); the worker refused the full Claude model ids the bridge itself maps (I1-3); the same-profile resume seed carried no model or effort (I1-6).

Landed later the same afternoon (master at `003fe29f`, 139 commits since the campaign commit): composer and shell commands (E-1 — the worker ran `bash` by name inside the profile's environment, whose PATH had none; E-3, E-5, E-6, E-7); viewer TLS and workspaces (G-1 — the CLI pins the exact certificate the daemon publishes for `[phone] tls_cert`; G-2, G-3, G-4); harness, review, and sub-agent turns (I2-7, I2-9, I2-5, I2-12, I2-15, I2-16); `mj acp` and the CLI (F-1, F-2, F-3, F-4, F-6, F-9, F-10, F-11, F-12, F-13, F-16, F-18); SSH, targets, and CLI from Track J (J-19, J-17, J-21, J-23, J-16, J-14, J-11, J-13, J-18, J-20, J-12, J-10, J-15). The last two groups were cut short by an API session limit after committing; their commits were cherry-picked and validated by the coordinator, with one hand repair where the 429 retry field met the pinned-TLS constructor rewrite.

Not fixed, with reasons: H-3 (which workspace `mj new` and the first dashboard launch share — a design decision held for the user; the option-1 commit sits on the fix branch); I2-1 (the Codex title comes from the injected memory block; moving the block or preferring Mjolnir's title is a design choice); I2-10 (capture before choosing a reviewer breaks six host tests' order); I2-14 (Muse question delayed ~3 min; logs for the window are missing); J-24 (mount source validation), J-25 (Codex quota error shown raw — work in progress in the group-11 worktree when it stopped), J-22 (docs for the `mj move` refusal); K-17's opt-out for Jev and semantic help search (a product decision, not a docs fix); E-8's right-edge title cut. I2-13 and C-11 are not defects.

Three integration test binaries failed once each under the parallel full-suite load and passed alone on every rerun: `mj-worker/tests/worker_environment.rs` (`checkpoint_worker_remains_visible_and_stoppable_after_clean_reexec`), `mj-controller` `web_viewer::tests::retry_uses_the_original_port_after_its_owner_releases_it`, and `mj-cli/tests/store_divergence.rs` (three tests). They bind ports or spawn daemons with fixed deadlines.

Next: re-verify the fixed findings' reproductions on fresh labs against the rebuilt master binaries (`<root>/bin-fixed/`), one track at a time, then close.

Open decisions for the user: H-3 (whether the first workspace is `default` or `mj new` follows the dashboard's directory-named workspace); whether to add an opt-out for Jev turn classification and semantic help search before launch (K-17).

### Re-verification (2026-09-23/24)

**R1 (tracks A–E, 19:26–19:58 on 09-23, binaries from `003fe29f`, seed 3301):** notes in `evidence/luna-manual-seed-3301-2541214/reverify-1/notes.md`. Every fixed A–E finding re-confirmed fixed except: A-12 regressed differently (R1-2, composer footer paints the first chord over the prefix label; fixed in `125232cb`); B-2/D-1 still failed for the Suspending state (fixed later in group 16); B-3 still named sessions by id in the suspended-destroy dialog and lifecycle notices (fixed in group 16); C-12's plural half (fixed in group 16). New: R1-1 (the New wizard pre-fills the directory `mj` started in; `a16ff9c6` says so), R1-3 (ASCII symbol set leaves "·" in the Subagents border; `b698ff82`). B-11's Enter half still reproduces (no fix).

**Merge of 09-24:** `origin/master` (20 commits, including the README rewrite, isolated clones, `--on-exit` for `mj acp`, the macOS CI test fix `e8afa5c9`) merged into the campaign's 13 commits as `d501dcf9`; `bin-fixed/` rebuilt from it (`SHA256SUMS-d501dcf9.txt`).

**R2 (tracks F, G, H, 08:02–08:45 on 09-24, binaries from `d501dcf9`, seeds 3401/3402/3403/3413/3423):** notes in `evidence/luna-manual-seed-3401-3191736/reverify-2/notes.md`. 26 fixes confirmed; all five Track H upgrade missions pass against the fixed build (2.15.0 mid-turn upgrade, worker pairing, two dashboards, downgrade protection, `Esc` on the old worker); H-1's waiting `mj` now uses about 1% CPU. Still failing: F-7 (`/options` says docker ready while doctor and the dashboard say unavailable) and the unknown-name half of H-3 (R2-5). Not re-verifiable here: the Import-tab half of F-18, the certificate-mismatch text of G-1. New: R2-1 (ACP cancel lost while the prompt is held), R2-2 (`daemon restart` during a session's start leaves it disconnected with no worker), R2-6 (doctor's `review.residue` flags running sessions' own clones and fails `doctor --json`), R2-11 (one destroyed session vanished from lookup, 1 of 4), R2-3/4/5/8/9/10/14 (wording and CLI shape), R2-7 (cli-reference gaps), R2-12 (an open 2.19 `mj wait` now fails with 503 during the upgrade instead of delaying it; accepted as the H-2 trade-off), R2-13 (viewer sends bursts of identical `/api/snapshot`; harm unknown). Runbook corrections: put `bin-fixed/debug` first on PATH rather than a shell function; scan processes with `ps` arguments, not `grep -qs` over `/proc/*/cmdline`; a cancel test must wait until the fake holds the prompt (`AFTER_FAKE=1`); the dashboard adds a directory-named workspace only when none exists, so H-3's expectation of one beside a CLI-made workspace was wrong.

**R2 fix waves (09-24):** R2-A landed R2-6 (doctor skips Mjolnir-managed checkouts and repositories with a live session), F-7 (`/options` runs the launch preflight's engine check, cached 30 min/60 s), R2-5 and R2-14 (unknown or missing `--workspace` lists the workspaces from the store without starting a daemon), R2-4 (400 for the reserved name), R2-3 (the CLI rewrites the API's field name to its flag), R2-9, R2-8 (dashboard-created sessions get `default_session_title`; `SessionRecord::listed_title` carries the F-12 fallback for the row and the viewer), R2-10, R2-7 (`c69618f4..aa89c000`). R2-B landed R2-2 (`5f732831`: a cancelled launch's rollback saves `Error` with its reason before removing the worker, so a daemon that exits during the 1 s owner wait leaves a resumable record, not `Disconnected` with no worker; resume's rollback still cleans up before saving) and R2-1 (`5dafd135`: the API withdraws a held prompt when `interrupt-turn` arrives during the hold, answering the prompt 409 "withdrawn"; the ACP adapter re-sends the interrupt every 250 ms until taken and reports `end_turn` for a turn that finished anyway). R2-11's cause is confirmed and not fixed: a session enters the SessionWiki index only on a sync pass (daemon start, after suspend or resume, stale search, hourly); destroy runs none and deletes the record and transcript, so a session created and destroyed between passes is never findable. Proposed: `wiki().sync_now(false)` before the destroy lifecycle in `daemon/resume.rs`, with a decision on bounding the wait behind a running first index build. Also found: the API suspend handler re-opens the store with `Controller::load()`, and its test reads the developer's real database (#1144).

**R3 (Track J on EC2 plus the group-16 behaviors, 12:01–13:22 on 09-24, binaries from `dd9e55ce`, EC2 `i-0d31a7cc6ff0cbe1b` for 69 minutes, cleaned up by ledger and confirmed by tag):** notes in `evidence/reverify-3/notes.md`. J-1, J-3…J-7, J-10…J-14, J-18, J-20 and J-9 fixed; J-17, J-19, J-21, J-23 fixed with leftovers; J-16 still fails (the client retry works but slow SSH creates outlast its 10-minute limit; cause R3-4, every SSH-bare session uploads its own 139 MB worker copy); J-22 docs confirmed; J-24 and J-25 unfixed and re-recorded (a non-existent mount source is saved and Docker creates it root-owned; the Codex quota error is still raw JSON in the system row). #1138's build-stamp check refused nothing. Group 16: mandatory `--workspace` and the Jev switch both work; help-search debounce passes. New: R3-1 (`mj wait` without `--turn` returns before the first prompt is answered), R3-2 (Continuation page ignores Jev off), R3-3 (any Setup save writes the built-in `[targets.docker]`/`[targets.podman]` blocks, after which doctor treats docker as user-configured and exits 1 on a host without Docker, undoing ecc10219), R3-4, R3-5 (dashboard git reads share the SSH masters uncounted and hit MaxSessions), R3-6 (host-key refusal reaches the CLI as a 500 with a log reference), R3-7 (running SSH sessions keep old `extra_args` after a restart), R3-8 (Jev off applies only to new sessions; the page says "nothing is sent"), R3-9 (quickstart and sessions.md still describe `.mj/worktrees` and `mj/<id>` branches; the product makes `.mj/clones/<id>` on the default branch), R3-10 (setup prints no smoke-test result), R3-11 (small leftovers). **The public install path is untested:** the harness's auto-mode permission check refused `curl … install.sh | bash` and the installer's PATH prompt on the host; the script was confirmed byte-identical to the repository's `install.sh` and the campaign binaries were used instead. Running the released binary on a fresh host needs either a permission rule for the subagent or the user doing it by hand.

**R4 (real harnesses: Claude, Codex, Muse, Kimi on instance `launch-r4`, 13:28–14:57 on 09-24, binaries from `dd9e55ce`):** notes in `evidence/reverify-4/notes.md` (the worker's own; its "Capture index" is authoritative). Fixed with captures: I1-6 (same-profile resume keeps `opus[1m]`, no lossy label), I1-13 (`/clear` after resume works), I1-15 (a failed analysis is reported as "Review failed"; the worker names the Bifrost version it needs), I2-12 (no `.bifrost` in the diff; a no-edit Muse turn says "Nothing to review"), #1136 (Kimi bootstraps in a Podman container with the installer text off the ACP stream), the Claude halves of I1-3 (full ids accepted) and I1-17 (interrupted marker in the pane and `mj transcript`), I1-11 on Claude and the idle-clear half on Codex, and the late-answer half of I2-5. I2-14 did not reproduce (form within 0.3 s; relay journal saved). Still failing: I2-7 (a never-prompted Codex session still cannot resume: "Codex has no native history … which this session has already used"; likely `native_session_may_have_history` treats a checkpoint-restored session as used), I1-12 in a changed form (R4-4), the unlisted-value half of I1-3 (R4-1), the command-text half of I2-5 (R4-10). Not exercised: I1-14 (Jev never chose to continue in 3 tries; threshold 0.90), I2-9, I2-15 and the Codex halves of I1-11/I1-17 (the Codex account was over its limit until 09-26; Kimi's weekly quota ran out mid-run because the Auto reviewer picked it). New: **R4-2 (blocks)** a `/model` change before any prompt made the Claude bridge send "Auto mode unavailable…" as agent text, the worker opened a turn that never ended, and `Esc`, `prefix+i` and `mj interrupt-turn` all failed (cancel dispatches "completed", execution "running"); **R4-3 (blocks)** that session could not be suspended (the 30 s settle timeout restarted the worker, the replacement exited with "no native history … already used", suspend failed with a broken pipe, row "Error / Recovery unavailable"); R4-1 (`/model not-a-model` accepted and silently becomes the default model); R4-4 (an accepted `/clear` leaves "You · /clear · Queued" pinned below later turns and "Delivery unconfirmed" after resume, Claude and Codex); R4-5 (a Codex session whose only turn failed on the usage limit cannot be suspended: "no session artifacts found"); R4-6 (a podman target without `image` stops the daemon from starting; unknown keys accepted silently); R4-7 (Kimi's weekly limit labelled `auth_required`); R4-8 (every worker logs a `skills_state` failure per minute for a 2.2 MB skill demo file over the 1 MiB limit); R4-9 (the automatic recovery copy took the lease so the review did not start; answers 8100bee6's open question); R4-10 (Kimi's Bash permission request carries no `rawInput`); R4-11 (a wizard-made Kimi session did not open; a spurious "Could not open Session … did not respond within 15 seconds" after a suspend); R4-12 (palette header, suspend and rename dialogs name a default-titled session by id); R4-13 ("Verdictt" typo in the review pane header); R4-14 (possible: `TYPESAFE_API_KEY` is in the ACP bridge environment, inside containers too, readable by the agent's tools); R4-15 (possible: Mjolnir-run Codex keeps state in the user's real `~/.codex`; the Import tab lists a native sub-agent thread). Runbook: podman targets need `image`; the daemon does not reload targets without a restart; container sessions need `--bundle` and a remote reachable from both sides (a `git daemon` on WSL's `lo` address worked); pin the reviewer profile so Auto does not spend a quota; worker `launch.json` and `acp-supervisor.json` hold an API key and must not be copied.

**Fix waves R5 and R3 (09-24 evening):** R5 landed `62a232fe` (SSH-bare sessions upload the worker once per host into the SHA-keyed cache the container path already used, then `cp` it per session; EC2 unchanged; fixes the R3-4 cause of J-16) and `ab7d7f49` (destroy indexes the session in SessionWiki first: an incremental sync bounded at 20 s, then a direct single-session write, with a deferred retry every 5 s for up to an hour when another writer holds the index; R2-11). R3 landed `f15ebc51..eea74e3f`: R3-3 (the Setup save no longer writes built-in targets it did not change, and doctor treats a block identical to a built-in as built-in), R3-1 (`mj wait` treats a pending `mj new` prompt as queued work), R3-2 (Setup shows Continuation off while Jev is off), R3-8, R3-7 (recovered SSH relays take the machine's current identity file and `extra_args` when host, user and port are unchanged), R3-6 (a host-key refusal reaches the caller as its own text), R3-5 (capacity probes retry; routine MaxSessions retries log at debug), J-24 (a newly attached mount source is checked locally or with `test -d` over SSH), J-25 (a Codex usage limit is one readable line, classified as a quota limit like Kimi's), R3-10, R3-9 (docs describe the `.mj/clones/<id>` clone; the import dialog's checkbox renamed to match), R3-11 (conversation title, `--branch` hint, `default` reason, stale SSH lock files removed at daemon start, Podman check wording). Full default-member suite green (42 binaries), clippy, fmt. Needs a live check: the stale-lock cleanup at daemon start and the relay argument refresh after `mj daemon restart`.

**Fix wave R4 (09-24 night, `c0f3babc..4c5f12f6` plus `95186525`):** R4-2 (the Claude bridge, claude-agent-acp 0.81.0, sends "Auto mode unavailable…" as agent text while answering a set-config request; the relay opened a turn for it that nothing ended and a stop had nothing to interrupt. Output that arrives during a SetConfig or SetSessionMode request no longer opens a turn, and a stop on a Claude turn waits 5 s for a result before the coordinator ends the turn itself), R4-3a (a notice no longer counts as native history, so a restarted worker starts fresh instead of exiting on "Resource not found"), R4-3b (a suspend or move whose worker restart leaves no worker restarts once more in checkpoint-only mode and takes the checkpoint from the relay journal and the target's files), I2-7 (a same-harness restore seeds `native_session_unused` when the transcript has no user message after the newest `/clear`, so a never-prompted Codex or Claude session resumes fresh), R4-4 (the `context-cleared` divider resolves the `/clear` command row and it is not saved for replay), R4-1 (a Claude value the bridge can only place on `default` is refused and the previous model kept), R4-5 (the artifact check for suspend looks at the current context only; "no session artifacts found" reaches the CLI as a sentence), R4-10 (a permission request with no input takes the command from the tool call's streamed text content; Kimi's jsonl is a checked-in fixture), R4-7 (a usage limit sent as `auth_required` is labelled a usage limit; `95186525` keeps Codex's readable line after the cherry-pick), R4-6 (`image` defaults for container targets; unknown keys warn once), R4-9 (a turn review waits up to 2 min for the automatic recovery copy), R4-11 (a finished launch reclaims a selection a refresh moved; a suspend no longer reattaches the Browse pane), R4-12, R4-13. Full suite green after the label fix. Open: adapter notices outside a config request still open a turn (the 5 s rule ends it); R4-3b's full suspend-after-failed-restart sequence and R4-11's wizard half need a live check.

**Fix wave R6 (09-24 night, `17a0618f..6168e39b`, plus `afc17876`):** R4-8 (an oversized or unreadable skill file is skipped and logged once; the canonical skills tree follows symlinks the way launch staging does), B-11's Enter half (a Rename or Close opened by its shortcut returns to the dashboard when it lands; hint text corrected), A-4/E-9 (the dictation chord says why it did nothing: helper missing, no Codex profile, no ChatGPT sign-in, or still checking), D-14 (a view still on its opening reveal is saved as following the tail, so a restored pane keeps following), I2-10 (the delta is captured before a reviewer is chosen; an empty capture answers "Nothing to review" with no reviewer; five host tests updated for the new order), I2-1 (first version: the worker drops a Codex-reported title for a session given project memory; being reworked in R7 so Codex keeps titling from the user's text). Conflicts on cherry-pick: `review_host/reviewer.rs` and `tests.rs` between R4-9 and I2-10 (combined: capture first, then `resolve_after_background_work`), and `acp/drive.rs` between R4-10's content tracking and I2-1 (title stripped first, then observed). `afc17876` rewrites the two R4-9 tests for the capture-first order and confirms they still catch the regression. Known gap under fix (R7 wave companion): the capture can itself be refused by the recovery copy's lease and is not retried. Full suite green apart from those two tests before `afc17876`; review host 23/23 after.

**R5 (final fake-lab re-verification of the R2-A/R2-B, R3, R5 and R6 waves, 22:11–22:52 on 09-24, binaries from `54925205`, seeds 3601/3602/3603):** notes in `evidence/reverify-5/notes.md`. 30 of 37 checks confirmed fixed with captures; 4 fixed with leftovers (A-4/E-9 R5-6, R4-12 R5-5, R2-2 R5-8, F-7 R5-3); B-3's notice half still fails (R5-4); B-2/D-1 and C-12 pass; R2-13 stays an observation (64 snapshot requests around one prompt, bursts of up to 7 in one millisecond). New: **R5-1** (a daemon isolated only by `MJ_DATA_DIR` uses the default instance's SSH socket directory, so every lab and any such user shares masters with the default daemon and, since `761279ef`, sweeps its stale locks; `default_control_dir` keys on the instance name, not `instance_identity()`), R5-2 (doctor's worker-binary checks read `config.targets` while the podman checks use the effective targets, so a config with no target blocks loses the container checks), R5-3 (Docker's not-installed text still raw; three texts for one fact), R5-4/R5-5 (notices name suspended and just-launched sessions by id: `session_notice_name` reads only the live map), R5-6, R5-7, R5-8 (a failed session's prompt refusal reads as "try again"), R5-9 (`mj acp --workspace nosuch` exits 0 silently with stdin closed; the ACP refusal is a -32603 with the text in `data`; `mj new --workspace nosuch` starts the daemon to refuse), R5-10 ("(s)" nouns). Runbook: export `MJ_INSTANCE` for every lab until R5-1 is fixed; `doctor --json` cannot exit 0 on a stock lab (its target blocks and the fake's missing sign-in); `mj setup` in a lab discovers the user's real harness homes, AWS identity and ssh hosts, so run it against a scratch config. Fix wave R8 (`evidence/fix-wave-r8-mission.md`) takes R5-1…R5-10.

**Fix wave R7 (09-24 night, `2d9ed5eb`, `b5ae15db`):** Claude Code's own `~/.claude/skills/synced` and `skills/.trash` are left to Claude Code (not collected, staged or installed; a stale copy in a session home is left alone because Claude Code owns that path), which drops the user's synced tree from about 4.1 MB to 355 KB under the 4 MiB archive limit; user's call on 09-24 ("let CC do its own sync of that"). I2-1 reworked: codex-acp 1.13.2's TitleGenerator joins every text block of the first prompt (no truncation) and asks a model for a title, and leaves resource blocks out, so the relay now sends its hidden context (memory, shell results, hand-off) to Codex as an embedded resource `mj://prompt-context`; other harnesses get the text block as before; `strip_hidden_prompt_context` removes codex-acp's wrapping in both forms; the `58c12e2f` title filter is reverted. Checked against the real bridge with a scratch `CODEX_HOME`: the model read the memory ("pong BLUEHERON") and the generated title came from the user's words. Costs: for Codex the Jev classifier no longer sees the memory text in the prompt; sessions already titled "Project memory instructions" keep it until renamed. Follow-ups in flight: Codex's own `skills/.system` treated the same way; profiles.md still says the sync skips symlinks (wrong since `17a0618f`).

**Skills follow-ups (`864ae4f8`, `92c34b27`):** Codex's own `skills/.system` is left to Codex like Claude Code's `synced`; Kimi, Grok and Muse keep their bundled skills outside `skills/` (checked in each binary and home; recorded in the commit). profiles.md and security.md now say links are followed (wrong in the docs since `c6072009`), oversized files are skipped and logged once, and why the 1 MiB file and 4 MiB tree limits exist.

**Fix wave R8 (09-25 early, `5cd9c555..25c6af83`):** R5-1 (`control_dir_identity`: "default" only when neither `MJ_INSTANCE` nor `MJ_DATA_DIR` is set; a data-dir override gets a 16-character fingerprint directory; the default instance's and named instances' paths are unchanged), R5-4/R5-5 (mj-cli's `session_notice_name` used `display_title` instead of `listed_title`, and a destroyed session's record vanishes before the reload, so the name is now taken when the operation begins), R5-2 (the worker-binary checks see the effective targets), R5-3 (one sentence per Docker case; the wizard and `/options` agree), R5-8 (a failed session's prompt refusal says it failed and why), R5-9 (unknown workspace refused plainly in `mj acp` and `mj new`; refused ACP requests carry the text in `message`; `mj new` no longer starts a daemon to refuse; a daemon-less `mj acp` still exits 0 silently with stdin closed, because no `session/new` ever arrives), R5-6 (the dictation reason goes through the shared notices and Recent messages; the bar is still one line), R5-7 (the worker poll refreshes open conversations, so the header retitles with the row), R5-10 (`counted` moved to `mj_core::text` and applied to every remaining "(s)" site outside the credential-sync poller and the sub-agent MCP text). Validated with the full default-member suite.

**R6 (fake-lab spot check of the R7 and R8 waves, 00:45–01:10 on 09-25, binaries from `921b52d9`, seeds 3701/3702 without `MJ_INSTANCE`):** notes in `evidence/reverify-6/notes.md`. R5-1 confirmed: a data-dir-only lab gets `$XDG_RUNTIME_DIR/mjolnir/<sha256(data dir)[:16]>`, and the default instance's directory, including its own stale lock, is byte-identical before and after. R5-2, R5-4/R5-5, R5-6, R5-7, R5-8 (wording), R5-9 (main cases), R5-10 fixed; R5-3 fixed with R6-3; the five regression items (R2-1, R2-11, R3-1, R3-3, B-11) pass. Skills sync has no CLI surface (unit tests only); Codex titles deferred to the real-harness run. New: **R6-1** (the R5-8 refusal recommends `mj resume` for a checkpoint-less failed launch, which is accepted and fails silently with "session has no checkpoint"), R6-2 (`mj acp --workspace nosuch` with the daemon stopped starts one to refuse; with no daemon record it exits 0 silently), R6-3 (the wizard's Docker row says "then Retry launch" before any launch), R6-4 (the `prefix+g` Mjolnir tab lists a default-titled session by id), R6-5 (the host-key refusal names `~/.ssh/known_hosts` even when `UserKnownHostsFile` is set). Runbook: an SSH-bare session to 127.0.0.1 as the user would write into the default instance's data directory (`~/.local/share/hel` is a symlink to `mjolnir`), so R5-1 was checked by forcing a host-key refusal; the daemon reloads targets without a restart for the capacity poller. Fix wave R9 (`evidence/fix-wave-r9-mission.md`) takes R6-1…R6-5.

**R7 (live checks: SSH-bare on EC2 `i-038b2f95ba42ac252` for 31 minutes, cleaned up and confirmed; real Claude on `launch-r7`; 00:47–01:45 on 09-25, binaries from `921b52d9`):** notes in `evidence/reverify-7/notes.md`. SSH-bare: R3-4/J-16 fixed (12 creates, 10 in parallel, all succeed; one 96 s cold upload then 11 cache hits and 250–350 ms copies; worst CLI time 92 s against R3's 7–10 minutes), J-17 (dashboard MaxSessions retries at debug), R3-7 (running relays and the resumed one take the new `extra_args`; sharding returns when the option is removed), stale locks removed at start and live ones kept, R5-1 for named instances, J-18, R3-6 all confirmed. Real Claude: R4-2 (no phantom turn on a model change), R4-1, R4-4, R4-5, R4-9 ("Nothing to review" in under a second on a no-change turn), R4-11, R4-8 (one warning per worker, no per-minute failure), and the Jev switch (no `TYPESAFE_API_KEY` in the harness environment; applied live) confirmed; R4-3's suspend fixed. New: **R7-5 (blocks)** a never-prompted Claude session, or one suspended right after `/clear`, cannot be resumed: the worker correctly opens a fresh native session (`resumed: false`) and the controller then rejects the new id in `resume.rs` ~701 ("ACP loaded native session X, expected Y") whenever `native_continuity` is set; likely I2-7's remaining Codex failure too. R7-1 (relay-proxy MaxSessions retry still at WARN), R7-2 = R6-5, R7-3 (a WARN per bare session per resource poll: hundreds of lines), R7-4 (a stopped daemon's SSH master kept alive by an open dashboard), R7-6 = R6-4. Runbook: the host's bare origin needs `HEAD` on the pushed branch; the daemon applies config edits live, so the "restart" steps are not what makes them take effect. Fix wave R10 (`evidence/fix-wave-r10-mission.md`) takes R7-5, R7-1, R7-3, R7-4.

**Fix wave R9 (09-25 morning, `78245c3a..8e3ea78d`):** R6-1 (the published `resume` capability only meant "not on the dashboard and no operation running"; `ViewerSession` now publishes `has_checkpoint`, the prompt refusal names resume only when one exists, and `POST /resume` on a checkpoint-less session answers 409 with the destroy hint instead of 202 followed by a silent failure; the phone viewer's Resume action still shows for such a session), R6-2 (`mj acp`'s start-time workspace check reads the store when no daemon runs, so both silent cases now refuse at once with exit 1 and start no daemon), R6-3 (pre-launch target checks for every kind say no "Retry launch"; move preparation still does), R6-4 (the Sessions dialog's Mjolnir and Live rows use the listed title; other `display_title()` sites remain: resume and move wizard titles, container dialog, combined view, workspaces, stopped sub-agents), R6-5 (the host-key refusal names the `UserKnownHostsFile` from the machine's args, or "the known_hosts file ssh uses" with `-F`).
