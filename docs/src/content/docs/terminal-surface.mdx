---
title: Terminal surface
description: Navigate the Mjolnir dashboard, resize panes, compose prompts, run commands, and use the command palette.
---

import { Image } from 'astro:assets';
import shotDashboard from '../../assets/screenshots/dashboard.svg';
import shotCommandPalette from '../../assets/screenshots/command-palette.svg';
import shotSetup from '../../assets/screenshots/setup.svg';

The Mjolnir TUI is a single control surface for conversations and the infrastructure behind them. It stays responsive while sessions launch, resume, stop, or run work in the background.

<figure class="shot">
  <a class="shot__zoom" href={shotDashboard.src} aria-label="Open the full-size screenshot: Mjolnir terminal dashboard">
    <Image src={shotDashboard} alt="Mjolnir terminal dashboard with a full-height Sessions sidebar beside Conversation, Prompt, Targets, and Quota" />
  </a>
  <figcaption>The Workspace switcher sits above Sessions, beside the conversation, prompt, targets, and quota. Setup lets you place the sidebar on either side.</figcaption>
</figure>

At startup, Mjolnir opens the session in your opened workspace whose agent spoke most recently and puts the cursor in Prompt. An empty workspace creates a session in the directory where you launched Mjolnir. Sessions shows every workspace and includes stopped sessions; opening a row switches to that conversation without changing its workspace ownership or where New creates sessions.

The **Workspace** switcher above Sessions shows the workspace you opened. Click it, or press `F3`, to open the workspace picker and choose where new sessions will belong. The switcher stays above the list on either side and at every sidebar size.

The picker supports mouse navigation: click a workspace to preview it, then **Open** to switch. **New**, **Rename**, **Recover**, and **Delete** manage the selected workspace; **Back** returns to the dashboard. Create and rename dialogs have editable name fields and submission/cancel buttons. Deleting a workspace with active sessions or recoverable drafts still requires typing its name.

Below the switcher, **New…** opens the session options wizard and **Quick new** creates a session using saved defaults (**Quick** in a narrow sidebar). **Resume** opens session history, and **Commands** opens the command palette. Commands remains available while you type and while a notice replaces the footer. Each session row has a **⋯** button for its actions. Displayed dashboard shortcut hints are clickable, including global shortcuts in the composer's footer. Help supports wheel scrolling and a **Close** button.

Click **Show stopped** at the bottom of the Sessions panel, or press `h` while Sessions has focus, to hide or show stopped sessions. The preference is saved and also appears in Setup. Stopped sessions remain available through Resume when hidden from the panel.

Open Setup (`F7`) and choose **Theme** to select Midnight (the default), Light, or Dracula. Saving applies the palette to the terminal dashboard, conversation, and dialogs and preserves it for future launches. The same preference is available as `theme` in [Configuration](/configuration/).

## Focus and scrolling

`Tab` moves focus through **Sessions → Prompt → Targets → Quota**. `Shift+Tab` moves in reverse. The transcript is not a tab stop, so focus changes never take the composer away merely to scroll conversation history.

Use `Page Up` and `Page Down` to scroll the transcript from any focused pane. A mouse wheel scrolls the surface under the pointer. Clicking a row selects it, and double-clicking a session opens it. Dragging across transcript or pane text copies that surface's selection to the available system or terminal clipboard.

Each list supports the same navigation keys:

| Keys | Action |
| --- | --- |
| `↑` / `↓` or `j` / `k` | Select the previous or next row |
| `Ctrl+P` / `Ctrl+N` | Select the previous or next row |
| `Home` / `End` | Select the first or last row |

Pane-specific actions are deliberately small:

| Focus | Key | Action |
| --- | --- | --- |
| Sessions | `Enter` | Open the selected session |
| Sessions | `n` | Immediately create a session with saved defaults |
| Sessions | `h` | Toggle stopped-session visibility |
| Sessions | `N` | New session with the full options wizard |
| Sessions | `s` | Stop immediately, without confirmation |
| Sessions | `r` | Restart with the same settings, without confirmation |
| Sessions | `d` / `Delete` | Delete after a Yes/No choice; `y` accepts, `n` cancels |
| Sessions | `Space` | Fold or unfold the selected project |
| Sessions | `1`–`9` | Fold or unfold the numbered project |
| Targets | `Enter` or `e` | Open target actions, including test and rename |
| Quota | `Enter` or `e` | Edit the selected profile configuration ID |

## Pane sizes

Sessions, Targets, and Quota each have minimized (`▁`), standard (`▪`), and maximized (`□`) title-bar controls. Click a control, or focus the pane and press `Alt+Z` to cycle through the available sizes. Max is hidden when it adds no space over Standard.

- A minimized Targets or Quota pane becomes a one-line summary.
- A minimized Sessions pane narrows the sidebar and shows one line per session.
- Only one support pane can be maximized at a time.
- Maximized Sessions widens the full-height sidebar. Maximized Targets or Quota receives available vertical space below Prompt.

Standard Sessions uses one third of the terminal width, bounded to 40–80 columns;
maximized uses half, bounded to 40–100 columns. Both stay within half the terminal
on narrow screens.

`Alt+G` toggles a dashboard preset. When all panes are standard, it minimizes Sessions, Targets, and Quota. When sizes have been customized, it restores all three to standard. `Tab` changes only focus and never changes a pane's size.

## Global keys

Function keys and `Alt` chords work no matter which dashboard pane has focus:

| Key | Action |
| --- | --- |
| `F1` | Open help; `?` also opens it when a non-Prompt pane has focus |
| `F2` | Open the command palette |
| `F3` | Switch workspace |
| `F4` | Show the web viewer address and access code |
| `F5` | Refresh all target capacity and profile quota data |
| `F7` | Open Setup |
| `Alt+N` | Open quick New with saved defaults |
| `Alt+W` | Open the full session creation wizard |
| `Alt+S` | Resume any non-live session through the resume picker |
| `Alt+A` | Mark all session activity as read |
| `Alt+X` | Cancel the selected session's in-flight launch, resume, or stop operation |
| `Alt+Z` | Cycle the focused support pane's size |
| `Alt+G` | Toggle the dashboard pane preset |
| `Alt+Q` | Detach this terminal while the daemon and sessions continue |

`Esc` is not a detach key. It closes a dialog or, from the conversation, cancels the active agent turn or shell command. `Alt+X` is different: it cancels a lifecycle operation such as launch or resume.

## Command palette

Click **Commands** or press `F2` to expose both shortcut-backed and palette-only actions. Commands for the selected session come first, followed by commands for the focused pane and then global commands. Click a command and **Run** to execute it, or **Close** to dismiss the palette. A session row's **⋯** button opens a palette limited to that session's actions.

<figure class="shot">
  <a class="shot__zoom" href={shotCommandPalette.src} aria-label="Open the full-size screenshot: Mjolnir command palette">
    <Image src={shotCommandPalette} alt="Mjolnir command palette with selected-session, pane, and global actions grouped together over the dashboard" />
  </a>
  <figcaption>Type to filter commands. Unavailable actions remain visible with the reason they cannot run.</figcaption>
</figure>

Use `↑` / `↓` or `Ctrl+P` / `Ctrl+N` to select a command, `Enter` to run it, and `Esc` to close the palette. Selected-session actions include Rename session, Container settings for container-backed sessions, Move, Stop, and Force destroy. **Move** opens a preparation and confirmation flow for a compatible profile or target; it keeps the logical session while rebuilding a fresh environment. Stop and Restart run without confirmation. Delete permanently removes the session, its target, and its recovery archive after a Yes/No choice. The palette is also how you discover useful commands that have no dedicated key.

## New session and Setup

Press `n` in Sessions or `Alt+N` from any pane to create immediately. The new session uses the directory where you launched Mjolnir and belongs to the workspace you opened, even if you are viewing a session from another workspace. Its normal Prompt receives focus when it is ready; no task-entry dialog or confirmation is required.

Quick New uses the saved agent and target defaults. With automatic defaults, it prefers a configured Codex account, usable local Podman, then Docker, then a local worktree. Plain directories use local execution. Set another account or target in Setup, or turn off **Focus prompt after creating** to keep focus in Sessions. Use `N` or `Alt+W` for the existing wizard when you want to choose all launch options.

Press `F7` to edit all configuration in Setup: accounts, targets and remote machines, projects, creation defaults, sidebar position, activity animation, review, and web access. Enter opens a section or edits a value. **Add** and **Remove** manage entries; **Detect machine** discovers installed accounts and usable local runtimes. **Save** or Ctrl+S validates and saves the draft. Errors keep your edits in place. Escape goes back, and Cancel discards unsaved edits. Web listener changes take effect on its next start.

<figure class="shot">
  <Image src={shotSetup} alt="Setup modal with all configuration sections and Save, Cancel, and Detect machine controls" />
  <figcaption>Configure Mjolnir without leaving the terminal or editing a file.</figcaption>
</figure>

## Prompt editor

The Prompt pane is a multiline editor with completion, history, and transcript controls.

| Key | Action |
| --- | --- |
| `Enter` | Send now, or queue while the active turn is running |
| `Shift+Enter` / `Alt+Enter` | Insert a newline |
| `Tab` | Accept a completion, or move to the next pane when none is offered |
| `Esc` | Cancel the running turn or shell command |
| `Page Up` / `Page Down` | Scroll the transcript |
| `↑` / `↓` | Move within multiline input or walk prompt history |
| `Ctrl+R` | Search prompt history; `Alt+R` changes the search scope |
| `Alt+T` | Toggle the transcript between rendered and raw text |
| `Alt+V` | Start or stop dictation when voice input is available |
| `Ctrl+V` | Paste the system clipboard |
| `Ctrl+A` / `Ctrl+E` | Move to the start or end of the line |
| `Ctrl+B` / `Ctrl+F` | Move back or forward one character |
| `Alt+B` / `Alt+F` | Move back or forward one word |
| `Ctrl+H` / `Ctrl+D` | Delete before or after the cursor |
| `Ctrl+W` / `Alt+D` | Delete the word before or after the cursor |
| `Ctrl+U` / `Ctrl+K` | Delete to the start or end of the line |
| `Ctrl+Y` | Yank the most recently killed text |
| `Ctrl+C` | Save the current draft to history and clear Prompt |
| `Ctrl+P` / `Ctrl+N` | Move to the previous or next line, then through history |

Inside history search, `Ctrl+R` or `↑` selects an older match, `Ctrl+S` or `↓` selects a newer match, `Enter` accepts it, and `Esc` or `Ctrl+C` cancels the search.

## Shell commands

Start a message with `!` to run the remainder through `bash -lc` inside the selected session's target and workspace:

```text
!git status --short
```

The command runs independently of an active agent turn, so it does not block the harness. Mjolnir streams bounded live output into the transcript and saves the result there. After the command finishes, its result is included once as hidden context with the next prompt you submit, letting the agent respond to what the command found without copying the output into Prompt.

Press `Esc` to cancel a running shell command. The same `!` syntax works in the [web viewer](/web-viewer/), where a Cancel button replaces the terminal key. A shell command has the permissions of the selected target, so review destructive commands just as you would in a directly attached shell.

## Help, footer, and terminal setup

Press `F1` for scrollable contextual help and `Esc` to close it. The footer is generated from the same command registry: pane-local commands appear first, `Alt` chords second, and function keys last. On a narrow terminal, the leftmost hints disappear first while the global function keys remain visible.

If `Alt` shortcuts arrive as `Esc` followed by a letter, configure the terminal to send Meta. In iTerm2 set the left Option key to **Esc+**; in Terminal.app enable **Use Option as Meta key**. Under tmux, a short escape delay keeps chords responsive:

```text
set -sg escape-time 10
```

For session states and recovery behavior, continue to [sessions](/sessions/). The [CLI reference](/cli-reference/) covers non-interactive commands, while [troubleshooting](/troubleshooting/) covers key handling, target failures, and viewer connectivity.
